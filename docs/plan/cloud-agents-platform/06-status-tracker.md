# 06. 当前状态与下一项

## 0. 当前主线（2026-09-13）

当前产品决定：[ADR-0032 / D-055](../adr/0032-infrastructure-admin-delivery-and-document-routing.md)。第一阶段完整交付基础设施＋Admin Web，之后才做用户 CloudAgents 对话。唯一工作顺序是 [04](04-extraction-and-migration.md)，完成定义是 [05](05-gates-and-acceptance.md)，Admin 功能/交互要求是 [07](07-admin-web-requirements-and-design.md)。

### 当前后续范围与下一项

用户于 2026-09-09 已启动 [ANYWHERE-RUNTIME-V1](05-gates-and-acceptance.md#anywhere-runtime-v1) Goal。
APP-M1-R1～R5 已完成；不扩大已通过的 BASE 范围，完整用户对话 UI 留在后续 U1。

当前核对基线为 `codex/cloud-agents-platform-p0` / `b7df543ce34c257852763429f9056fd2e6877de9`，保留原有 dirty work。
候选 `0.3.0-dev.249` 已在真实 Docker、outbound RemoteWorker 与三节点 Kubernetes 完成四 Provider 的 Workspace/Sandbox、运行中恢复、跨节点接管、故障矩阵、Admin 与仓外 TS/Go SDK 闭环；仅固定工具链版本不一致导致的完整 contract/SDK 生成检查保持为环境边界。

| 核对项 | 当前结论与边界 |
| --- | --- |
| Provider 接入 | [distribution](../../../packages/cloud-agent-distribution/src/index.ts) 已注册 Codex、Claude Code、Pi 与 deepseek-harness；Pi 固定官方 SDK 0.85.1，deepseek-harness 固定 SDK/CLI 0.1.2-rc.1。候选 `0.3.0-dev.249` 的四 Provider × Docker/RemoteWorker/Kubernetes 核心链路、事件续读、交互授权、长任务、退出恢复和跨节点接管均已真实通过 |
| Agent 使用底座 | [durable runtime execution](../../../services/control-plane/internal/managedagent/durable_runtime_execution.go) 已按会话直接绑定 Foundation Workspace/Sandbox/generation，并保留旧 EnvironmentLease/WorkerEndpoint 兼容路径；当前候选已实测四 Provider 的 Docker/outbound RemoteWorker/Kubernetes Workspace/Sandbox、事件续读和进程重启恢复 |
| 运行中恢复 | product-000092 已增加持久 claim、租约续期、Agent checkpoint、交互与工具回执、未知副作用对账和 fenced takeover；候选四 Provider 均已真实覆盖 Docker/outbound RemoteWorker/Kubernetes 进程重启恢复，并在独立 Docker daemon、RemoteWorker 节点和三节点 kind Kubernetes 完成运行中 Turn 跨节点接管；旧 writer 先 fence，未知副作用先对账，RPO 均为 0 |
| 跨节点数据 | product-000091 已增加经摘要验证的 `portable-tar-v1` 快照：源 Workspace 停止并释放 writer 后写入受管共享目录，源 Target/卷不可用时可恢复到另一 ready Docker/Kubernetes Target；数据库拒绝旧 writer 重新取得写权。四 Provider 的 outbound RemoteWorker 源节点失效、快照恢复与目标重绑定已真实通过 |
| SDK 与部署 | TS/Go 会话、Execution 恢复与副作用对账契约已生成；Go response shape 会保留恢复后的 Foundation Session binding。Agent-enabled Compose/Helm 才挂载凭据，默认 no-Agent 部署保持不变。仓外 TS/Go 精确候选安装与真实调用、三环境矩阵和相关 Admin 验收已通过 |
| 已有实测 | R2 已在真实 OrbStack Docker 上完成四 Provider 基线；当前候选独立完成四 Provider Docker/outbound RemoteWorker/Kubernetes 核心链路与三环境跨节点 Turn 接管，均包含未知副作用对账。历史[外部客户节点](evidence/base-m5-external-remote-worker-20260909-r1/README.md)与[Kubernetes 故障](evidence/base-m5-kubernetes-fault-soak-20260909-r1/README.md)仍只属底座证据，不用于填充本次 Provider 级格 |

APP-M1-R1～R5 均已按新验收完成，`ANYWHERE-RUNTIME-V1` VERIFIED。下一项为完整用户对话 UI（U1）；执行顺序和可复制 Goal 仍只在 [04](04-extraction-and-migration.md#anywhere-runtime-plan)。
本轮使用用户更新的第三方配置，未调用官方 OpenAI/Anthropic API；缺凭据、外部节点授权或实测的条目保持开放，不从历史客户节点记录外推新授权。

### APP-M1-R1 实施与验收记录（2026-09-09）

- product-000089 将 managed Agent Session 直接绑定 ready Foundation Workspace/Sandbox/generation、DeploymentTarget、Operation、RuntimeProfile、EnvironmentProfile 与 Release；服务端和生成 TypeScript/Go SDK 同步支持新绑定，并保留旧 Lease 兼容和互斥校验。
- Control Plane 复用 OpenSandbox PTY/Files API，在 Sandbox 内启动统一 Runtime；凭据经标准输入写入匿名文件描述符，Provider state 保存在长期 Workspace。Codex 与 Claude 后续 Turn 均恢复同一 Provider session；超时上限提高到 30 分钟，握手/Ready 仍有独立 deadline。
- 真实 OrbStack Compose 使用受限 EnvironmentProfile，只允许凭据 `baseURL` 的目标主机；Codex/Claude 均完成初始 Turn、工具写文件、Files API 摘要、ArtifactCandidate 与后续 Turn，且没有回退官方 endpoint。旧 Lease/no-Agent、Control Plane 重启、备份恢复及 Admin/User smoke 同轮通过，测试自有 Sandbox、容器和卷清理为零。
- PostgreSQL 17.6 已通过 product-000088→000089、fresh install 与 replay；迁移校验明确采用数据库字节序规范化网络目标。候选包 `0.3.0-dev.122` 仅在本机生成，未发布。

### APP-M1-R2 实施与验收记录（2026-09-09）

- distribution、能力目录、生成 TypeScript/Go SDK、Compose/Helm 凭据挂载与 Admin/User Provider 选择已覆盖四个 Provider；product-000090 启用 Pi 与 deepseek-harness，PostgreSQL 17.6 的 product-000089→000090、fresh install 与 replay 均通过。
- Pi adapter 固定官方 `@earendil-works/pi-coding-agent` 0.85.1，并使用其 SessionManager 持久恢复；deepseek-harness 固定官方 SDK 与 CLI 0.1.2-rc.1。该版 SDK 没有 reopen 接口，所以能力目录如实标记 `resume-session: emulated`、`read-history: unsupported`；平台在受管 Provider state 中原子保存有界权威消息历史，校验 Provider/版本后为后续 Turn 创建新的 SDK 会话，历史缺失或损坏时拒绝继续。
- 未发布候选 `0.3.0-dev.129` 在 `linux/arm64` OCI 检查中核对四个 Runtime Describe、Codex CLI 0.150.1、Claude Code 2.1.207、deepseek-harness 0.1.2-rc.1 与 product-000090；Runtime、Deployment、Migrations 摘要分别为 `sha256:a17ac0c26b1a22ef0d240ac071c183c1dd084f66d4f574d37330db7a200abda0`、`sha256:1cc2e67cad56b3156f4547cc8d22cf86e8e73b17fa32b378db08f116a5fb7fa8`、`sha256:9c65e81112f30168d18a967cfe71890fe14efcbef70cd8ee729607b8f0244cb9`。
- 同一候选的真实 OrbStack Compose 验收使用凭据指定的三方 `ai.191027.xyz`，四个 Provider 均通过初始文件 Turn、Artifact 精确下载、事件终态、跨 Runtime 进程后续 Turn、Stop/保留/Rebuild；Admin/User 浏览器、普通用户 Admin 403、默认 no-Agent 和 BASE 受影响回归同轮通过。测试自有容器、镜像、卷和网络清理为零；未发布候选、未使用官方 API、未把构建或 Mock 当作验收。

### APP-M1-R3 实施与验收记录（2026-09-10）

- product-000091 将 Snapshot backend 扩展为 `portable-tar-v1`：Controller 以未启动 helper 从已停止且释放 writer 的 Docker Workspace 生成确定性 tar，原子写入受管共享目录并保存规范化 SHA-256；恢复前重新校验 archive 摘要、归属和一致性点，在目标 Target 创建新卷。恢复 acceptance 锁定并复核源状态，跨 Target restore 存在后数据库 trigger 拒绝旧 Sandbox 恢复 writer；legacy `docker-volume-v1` 继续只允许同 Target。
- 两 daemon 物理实测先在一次性 Docker-in-Docker 源节点写入并导出 Workspace，再删除整个源 daemon，随后在 OrbStack 目标节点只凭 portable archive 恢复；目标内容摘要与源一致，RTO 640 ms、RPO 0 byte，源节点、目标卷和 archive 均清零。证据 `.tmp/anywhere-runtime-r3-physical-r5/evidence.json`，SHA-256 `0463ed7ed8409ba031486952aa5509630f0afd4e2346770eb0f83fef445588eb`。
- 数据库权威闭环在 PostgreSQL 17.6 / Docker 29.4.0 上把源 Target 标为 unavailable、删除源卷，再经生成 SDK/Admin API 恢复到 `target-restore`；旧 writer 重取被 SQLSTATE 23505 fence，真实 OpenSandbox 读回原 proof，RTO 6714.51275 ms、RPO 0 byte。真实 Chromium 发出 16 个 Admin 请求并显示 portable backend、来源 Target 和跨 Target 目标 Profile；普通用户为 403，资源版本冲突为 409。证据 `.tmp/anywhere-runtime-r3-cross-target-r4/evidence.json`，SHA-256 `1556d13678ca3d1cc81ab5fb9a58948f50e36dd0eccb8252c1bc16d3dd05f3a4`。
- 迁移已通过 fresh、000090→000091、no-op replay 与生成检查；product-000091 摘要为 `sha256:f51cfb6e3c25a33fc3d4ef26c0396f38a14c4392ae3bcbe6e63b90efb4ab8394`。契约标准、TypeScript/Go Snapshot 投影、Admin typecheck/测试和相关 Go 包通过。
- 未发布候选 `0.3.0-dev.133` 固定 product-000091，Runtime、Deployment、Migrations 摘要分别为 `sha256:9826ec0e671cdbe6be37e279689458f4d364f0fb2280dd9d5b3a55cb02ea93c3`、`sha256:4a0024e44a145b6e35f3c0e883f341be3d7e49f00962e92b2e98902dbc20ba9a`、`sha256:045bdff91444d7f278d16aebd2b526343bad5b6060963cbb83ac1ebb333ecc35`。部署包使用 content-addressed Web index，避免确定性归档被 Docker cache 复用成新 JS/旧 HTML；同候选默认 no-Agent、显式 Managed Agent、浏览器、Gateway、备份恢复和零残留 Compose 验收通过。
- 同候选带三方凭据的附加回归已到达真实 Codex 成功终态，但该次模型没有生成约定文件的 ArtifactCandidate，因此不计为四 Provider 通过；R2 固定候选的四 Provider Docker 证据保持有效，R5 仍须对最终候选完成十二格与故障矩阵。R3 只证明新测试 Workspace 的 Docker 跨节点数据恢复，不声称进程内存热迁移、跨 Region 灾备或运行中 Turn 已接管。

### APP-M1-R4 实施与验收记录（2026-09-10）

- product-000092 为 running Execution 增加数据库持久 claim/续租、attempt、checkpoint、Provider transcript、交互与副作用待对账状态；Control Plane 不再以进程内 active map 作为执行所有权。旧 claim 或 generation 的 checkpoint、事件和终态回执不能覆盖新 attempt，未知副作用保持 `awaiting_reconciliation`，只有匹配 generation 与 checkpoint digest 的显式核对可继续。
- Foundation portable restore 到新 Sandbox running 后，数据库在同一事务校验 EnvironmentProfile、RuntimeProfile、Release 和目标可用性，再把 active Session 原子重绑定到新 Workspace/Sandbox/generation；不兼容目标拒绝，源 Sandbox writer 已释放且旧卷删除。Go SDK 生成模板已修复 Foundation Session 字段被 response sidecar 剥离的问题，并由真实候选 CLI 解码夹具与生成测试覆盖。
- 候选 `0.3.0-dev.141` 在两个独立 Docker daemon 上运行真实 Codex Turn：工具副作用完成后 Control Plane 被 SIGKILL，随后停止源 Sandbox 并确认 `writerReleased=true`、生成 portable 快照、删除源卷和源 OpenSandbox；目标 daemon 恢复 Workspace/Sandbox。claim 到期后的第一次重试因副作用结果未知被阻断，文件摘要核对为已应用后显式 reconcile，attempt 2 以 `cross-node-takeover` 完成，来源/目标 ID 分别为 `docker-compose-target` / `docker-compose-target-restore`。
- 本次 RTO 3229 ms、RPO 0 byte、快照 3190272 bytes；同轮 User/Admin 浏览器、备份恢复与 Compose smoke 通过，测试容器、卷、网络残留均为 0。证据 `.tmp/anywhere-runtime-r4-cross-node-20260910-r1/evidence.json`，运行日志 SHA-256 `94cd15ec4cf40dd883a4b0d9e1223bd4743f23b7388f1173bb3d9eb72044a476`。
- 候选 Runtime、Deployment、Migrations、TypeScript SDK、Go SDK 摘要分别为 `sha256:6169ee18eea567579732f9874a0e5e79d61aa55498ad91e94f9a1d89a2042f2f`、`sha256:454dc0e1e45a065f0a69e8fc8cc3c5967315501effc17f598aedab135d41b910`、`sha256:b9025945bf43a5e9496f6aa6bfcbe8b2ec7cb030ad075faabebcd7d78c5f3690`、`sha256:e3a48e3eadc5a53082b9c7f9f4009b49aed4d2703dbcbdefc3b72dd1059b7473`、`sha256:2831589103cc9424e0f1746770c7bc2a6ac5d95f4372fad67e66b49e8c9f6b19`。该候选仅在本机生成，未发布；四 Provider × 三环境和完整故障矩阵仍由 R5 验收。

### APP-M1-R5 实施与验收记录（2026-09-12，进行中）

- Codex 的 Host-owned `workspace.write_text_file` 现以原子写入、路径/symlink 约束和工具回执屏障接入 app-server；真实 app-server 会在 Host 回执前发送 `dynamicToolCall`/Turn completed，Runtime 因此按 item ID 暂存完成通知，待 Host 回执后再结算。Foundation 启动命令会把通过 1～30000 ms 校验的内部回执延迟传入沙箱 Runtime，避免只配置外层 Worker 而未覆盖实际 Provider 进程。对应 Codex 33 项测试、control-plane managedagent Go 测试、脚本语法和 `git diff --check` 通过。
- 未发布 dirty-source 候选 `0.3.0-dev.173` 使用更新后的三方 Codex/Claude Code 配置，在真实 OrbStack Docker/OpenSandbox 中命中运行中工具副作用窗口：文件已写入且 checkpoint 标记 `pendingSideEffect=true` 后，Control Plane 被 SIGKILL；源 Sandbox 先 Stop 并释放 writer，再生成校验过的 portable snapshot，删除源卷和源 OpenSandbox，最后在另一 Docker daemon 的 Target 恢复 Workspace/Sandbox。
- claim 到期后的自动执行先停在 `awaiting_reconciliation`，没有盲目重复工具；平台核对恢复文件摘要后以 `confirmed` 对账，attempt 2 通过 `cross-node-takeover` 完成。实际来源/目标为 `docker-compose-target` / `docker-compose-target-restore`，RTO 3303 ms、RPO 0 byte、快照 3115520 bytes；同轮 User/Admin 浏览器 smoke 通过，Compose/OpenSandbox 容器和卷均清理为 0。日志 `.tmp/anywhere-runtime-r5-compose-r30-recovery-only.log` SHA-256 为 `169c5beaab3380614200a0dad81e9b0012d8ddf31bbba3f8f40ed32f1babdba2`。
- 该候选 Runtime、Deployment、Migrations、TypeScript SDK、Go SDK 摘要分别为 `sha256:7ca6b73478487c386752fee0288448584ead69b1cc8c67d33896ed11b72d4a74`、`sha256:af7a9427a6a79ce1d18ceb921fcb1badc7b63ba589506c1d5bd79af73e0de3ef`、`sha256:e99a4cc32a2e5dec415580f8821b12f86502ff643460bd0a65d599fbd2ad3074`、`sha256:41d87e69311e57601bd7de0416f17b83c4384d289a0aafedadd399366a0fb51a`、`sha256:6e1557b77ec0e11b2827dff596962376d3dbc2f0420f759fa7fb7e959986873b`。Docker 的 Codex/Claude Code/Pi 当前候选真实流程已通过；deepseek-harness 三次均在无副作用 checkpoint 前收到三方上游 `ip_rate_limit_exceeded`，按安全规则创建新 Turn/Execution 重试后仍受限，未计为通过，也未继续施压上游。deepseek-harness 其余环境格、运行中恢复与跨节点接管、完整故障矩阵、TS/Go 仓外消费和长任务仍保持开放。
- 同一候选使用更新后的三方配置，连续完成 Codex/Claude Code/Pi 的 Docker 与 outbound RemoteWorker 初始文件 Turn、精确 Artifact 下载、增量事件终态和读取同一长期 Workspace 的 follow-up Turn；同轮 Docker 跨节点接管在旧 writer 释放后完成 attempt 2，RTO 3319 ms、RPO 0 byte、快照 7471616 bytes。User/Admin 浏览器 smoke 通过，测试容器、卷和 OpenSandbox 资源均为 0；日志 `.tmp/anywhere-runtime-r5-compose-r30-three-providers.log` SHA-256 为 `044b8db24db9287bc726f2ce998aa971967bd1a9e551d77003f38d6a2ced31d1`。
- Compose 验收脚本新增显式 Kubernetes Runtime 路径：只接受明确 kubeconfig/context，使用唯一 ownership label 创建隔离 namespace、最小 RBAC、固定 OpenSandbox controller/execd/egress 制品和长期 PVC；Control Plane 通过既有 TLS 代理访问真实 Kubernetes OpenSandbox，不放宽非 loopback HTTP 校验。真实 OrbStack Kubernetes 的 Codex 已完成新 Workspace/Sandbox、文件工具副作用、Artifact、事件流、follow-up 恢复，以及 Stop 删除计算/保留单个 PVC；同轮 Docker 进程重启恢复先等待旧 claim 到期、对账后 attempt 2 完成。最终本轮 namespace、cluster RBAC、Kubernetes 运行时容器、Compose 容器/卷/网络均为 0；日志 `.tmp/anywhere-runtime-r5-compose-r30-kubernetes-codex-20260910-r3.log` SHA-256 为 `dd0c6b54ea049019db2c0a98f250482ae3367a422a1899cf9d5ec90f68cf2bec`。随后同一路径完成 Claude Code 与 Pi：两者均在真实 Kubernetes Pod 内完成初始文件 Turn、精确 Artifact/事件终态和读取同一长期 Workspace 的 follow-up Turn，Stop 时仅保留一个 Bound PVC，最终精确清理 namespace、RBAC、Pod/OpenSandbox 容器、Compose 容器/卷/网络为 0；日志 `.tmp/anywhere-runtime-r5-compose-r30-kubernetes-claude-pi-20260910-r4.log` SHA-256 为 `a2c9adde4d537532ab431a85624cbd6c6b7973807014487ee197ebfc53d04ba1`。
- 使用更新后的第三方配置，真实 RemoteWorker Pi 运行中恢复已通过：Control Plane SIGKILL 后等待 claim 到期，第一次重试停在 `awaiting_reconciliation`，核对 Workspace 文件摘要后 attempt 2 以 `process-restart/recovered` 完成；同轮 Docker Pi 也通过，RemoteWorker 与 Docker 资源均精确清理为 0。日志 `.tmp/anywhere-runtime-r5-compose-r30-remote-pi-recovery-20260910-r3.log` SHA-256 为 `0eda7c67ce268dcd1f64ee85d7a886906dc007587752af6c9d8ae3570acbe3c2`。
- 使用更新后的第三方配置，真实 Kubernetes Claude Code 与 Pi 运行中恢复复跑均通过：两者都在真实 Pod 内完成 pending side-effect checkpoint、Control Plane SIGKILL、`awaiting_reconciliation`、摘要核对、attempt 2 `process-restart/recovered` 和最终文件读回；本轮 namespace、RBAC、Pod/OpenSandbox、Compose 容器/卷/网络均精确清理为 0。日志 `.tmp/anywhere-runtime-r5-compose-r30-kubernetes-claude-pi-recovery-20260910-r5.log` SHA-256 为 `537ba931ba5c16b391d64859e60871b9114cd623ca0ef9aa48816eb82d859f89`。
- 仓外 SDK 验收已通过：`bun scripts/test-platform-sdk-consumers.ts` 在隔离临时目录以 HTTP artifact/proxy 安装精确候选，Fresh TypeScript consumer 与 `GOWORK=off` Fresh Go consumer 均完成编译和真实 Control Plane/Worker loopback 调用，无 workspace/file/git 依赖；日志 `.tmp/anywhere-runtime-r5-platform-sdk-consumers-20260910-r1.log` SHA-256 为 `e59b8ccbca5bb03b95d662c0f69cd7a7b71e8bcd8e25dd33ce024716a9403d03`，TypeScript artifact `sha256:4abd47e966daa6f6768b5f13fbc85b9da97771ac0847111441ddc708f4fada4b`，Go module zip `sha256:adfaddf7dd8da8e91fcbbe04020bfe4650cffdd87310cf0d1530ecf02fecaa0a`。
- 真实 Provider 恢复测试原先只在跨节点模式注入 12 秒 Host tool receipt 延迟，却在所有真实凭据模式执行，导致普通运行可能在 checkpoint 采集前结束；现统一按真实 Provider 测试启用该内部延迟，Foundation Runtime 继续通过已校验环境变量传入实际 Provider 进程。脚本语法与 `git diff --check` 通过。
- 用户更新 `tenant-local.codex.json` 与 `tenant-local.claudeAgent.json` 后，重新用三方 endpoint `https://hy.apiclaw.cc/v1` 直接验证 `/models`、Claude `/messages` 与 Codex `/responses` 均为 HTTP 200；随后以同一配置目录运行候选 `0.3.0-dev.173` 的 Docker Codex + Claude Code 真实闭环与 Control Plane 重启恢复均通过，日志 `.tmp/anywhere-runtime-r5-compose-r31-updated-codex-claude-docker-20260910-r2.log` SHA-256 为 `372c353db7c962f416fd8bdb3bea13652387bd967e6ae3435c3394d67792ebb2`。
- 在保留冻结 product-000092 原始字节的前提下新增 product-000095 后继迁移：修正交互解析中的 `GREATEST` 调用，并让取消/中断终态清空 `runtime_messages` 以满足 000092 约束；生成器 `--write/--check` 均通过。未发布候选 `0.3.0-dev.177` 使用更新后的三方 Codex/Claude Code 配置完成真实 Docker 初始 Turn、文件副作用、Artifact、事件与 follow-up、Control Plane SIGKILL 后 process-restart/recovered（Codex 与 Claude）以及 Approval、User Input、Cancel、Interrupt 交互验收；`Agent approval and user-input real E2E passed` 与 `platform Compose smoke passed` 均出现，Docker 容器/卷/网络清理为 0。日志 `.tmp/anywhere-runtime-r5-compose-r35-updated-codex-claude-interactions-20260910-r1.log` SHA-256 为 `5ece01f5820074a47090f2fb5f638f9df379d37f9b45fa18c277741da9f3e8dc`。该候选 Runtime、Deployment、Migrations、TypeScript SDK、Go SDK 摘要分别为 `sha256:7ca6b73478487c386752fee0288448584ead69b1cc8c67d33896ed11b72d4a74`、`sha256:b228547ccbb7f32c5fcfd818de075f5998d2f8220dabc29bdc3aa1fd526e1ebc`、`sha256:da6b5760dc26434b19bbc034c1b9e532029dfa61e0c4ae26e4cedb3ab8a519f5`、`sha256:d2c8c158820de37513241b1202a88435e94dcdc661feaa7afb05bd564e89478a`、`sha256:6e1557b77ec0e11b2827dff596962376d3dbc2f0420f759fa7fb7e959986873b`.
- 同一未发布 `0.3.0-dev.177` 候选继续使用更新后的三方 Codex/Claude Code 配置完成隔离 Kubernetes Claude Code 运行中恢复：真实 Pod 内 pending-side-effect checkpoint、Control Plane SIGKILL、claim 到期后的 `awaiting_reconciliation`、Workspace 摘要核对、attempt 2 `process-restart/recovered`、文件读回，以及同脚本 Docker Claude 恢复均通过；`platform Compose smoke passed`，进程退出码为 0，namespace/RBAC/Pod/OpenSandbox/Compose 容器、卷、网络均精确清理为 0。首次同范围复跑的业务标记虽成功但清理路径退出码为 1，未计入；第二次日志 `.tmp/anywhere-runtime-r5-compose-r35-updated-claude-kubernetes-recovery-only-20260910-r2.log` SHA-256 为 `9bb61c110753f96e95f68aec3e8f368ae721b854c8f10a58852dc8d060056c3d`。
- 用户更新后的 `tenant-local.codex.json` 与 `tenant-local.claudeAgent.json` 再次通过不输出密钥的第三方直连探针：`https://hy.apiclaw.cc/v1/responses` 与 `/messages` 使用 `gpt-5.6-luna` 均返回 HTTP 200 和 `OK`；两份文件为 `0600`，与本轮 smoke 使用的对应配置字节一致。最新 Docker/Kubernetes 实测日志未出现官方 `api.openai.com` 或 `api.anthropic.com` 端点命中。
- 同一未发布 `0.3.0-dev.177` 候选以更新后的 Codex/Claude Code 第三方配置完成 Docker 事件断线续读实测：每个 Provider 先从持久 `nextCursor` 启动 `events watch`，在收到首个续读事件后强制中断，再用同一游标恢复至 `execution.complete`；两次恢复各严格对账 4 个 UID，无缺失或重复，并与既有 Control Plane SIGKILL 后 `process-restart/recovered`、Approval/User Input/Cancel/Interrupt 和精确清理同轮通过。日志 `.tmp/anywhere-runtime-r5-compose-r35-updated-codex-claude-events-20260911-r1.log` SHA-256 为 `sha256:be2becfe05b3b691aa9e9526a64eafa0f42fa469b20961d585475859dd8ba038`。
- 同一候选在 Docker 真实交互脚本中完成超过短 Turn 时长的 Claude Code 长任务：两个连续 35 秒 Bash 阶段均完成，Execution 成功终态实测 `duration_seconds=79`，随后 Approval/User Input/Cancel/Interrupt 仍通过；日志 `.tmp/anywhere-runtime-r5-compose-r35-updated-codex-claude-longtask-20260911-r4.log` SHA-256 为 `sha256:539cab0400e27bbf63b7b90163678200ba61a36343e9357d36ebbf3403812baa`，资源精确清理为 0。此前 r1 的 Codex 未实际执行长命令、r2 在进入长任务前发生 TLS handshake timeout、r3 长任务已运行 80 秒但测试错误依赖未公开的原始 Bash 字符串，均未计入最终通过。
- 同一候选以本地最新 `tenant-local.codex.json`/`tenant-local.claudeAgent.json`（两者均为三方 `https://hy.apiclaw.cc/v1`、`gpt-5.6-luna`，并与隔离测试目录逐字一致）重跑 Docker Codex/Claude Code：真实事件续读各严格恢复 4 个 UID，Control Plane SIGKILL 后两者均以 `process-restart/recovered` 完成 attempt 2，81 秒长任务、Approval/User Input、备份恢复和 Compose smoke 均通过；退出码 0，Compose 容器/卷/网络均为 0。日志 `.tmp/anywhere-runtime-r5-compose-r35-updated-codex-claude-rerun-20260911.log` SHA-256 `sha256:45bcedc97d8a9147fbad8e3c17837ea53e4ca1878fb9772a87878ab76fea9bb5`。该次只更新 Docker Codex/Claude 证据，不扩大 R5 的 Kubernetes/RemoteWorker/deepseek-harness 缺口。
- 以同一最新三方 Codex/Claude 配置重建候选 `0.3.0-dev.179`（含 product-000096 Kubernetes snapshot/restore 与 Helm snapshot PVC），再次完成 Docker 真实事件续读（两次各严格恢复 4 个 UID）、Control Plane SIGKILL 后 Codex/Claude `process-restart/recovered` attempt 2、84 秒长任务、Approval/User Input、备份恢复和 Compose smoke；退出码 0，Compose 容器/卷/网络精确清理为 0。候选产物 22 项 SHA-256 校验通过；日志 `.tmp/anywhere-runtime-r5-compose-r37-final-20260911.log` SHA-256 `sha256:1a9d37fe6a615017b9a795ba5b5cd6ca04dd0a8bd51885d729b3bb26a842dea9`。本轮仍只证明 Docker 进程重启恢复，不把 Kubernetes portable snapshot 或 outbound/Kubernetes 跨节点接管计入 R5。
- 在修正 Kubernetes v5 stdin close、`pods/exec` RBAC、helper Pod 终止宽限、非 root tar 权限及 exec 无 status 误判后，重建未发布 dirty-source 候选 `0.3.0-dev.182`；22 项制品校验通过。Runtime、Deployment、Migrations、TypeScript SDK、Go SDK 摘要分别为 `sha256:7ca6b73478487c386752fee0288448584ead69b1cc8c67d33896ed11b72d4a74`、`sha256:b7535446f486c0910db66afd1f1fd514eefd8b3263f7d2f31cca655f366085eb`、`sha256:2b2502456d770b016e61bcac4d5d1fb9a85ba79f5eae5d6168e814786cbee8d9`、`sha256:24f6ab1aa37771a138046450d8cdb963136a524fe88b51a669017680898586b5`、`sha256:6e1557b77ec0e11b2827dff596962376d3dbc2f0420f759fa7fb7e959986873b`。候选仅本机生成，未发布；Kubernetes 跨节点和其余 R5 缺口保持开放。
- 候选 `0.3.0-dev.182` 使用最新 `tenant-local.codex.json`/`tenant-local.claudeAgent.json` 三方 endpoint，在 Docker 真实闭环中完成 Admin/User 浏览器 smoke、两次事件续读各恢复 4 个 UID、Codex/Claude Code Control Plane SIGKILL 后 `process-restart/recovered` attempt 2、85 秒长任务、Approval/User Input、备份恢复和 Compose smoke；退出码 0，容器、卷、网络精确清理为 0。日志 `.tmp/anywhere-runtime-r5-compose-r40-final-updated-codex-claude-20260911.log` SHA-256 `sha256:67e3f8e88cbf51836fec973cd77a27cccca59004dc71ac37cb9ed14e7803f9ea`。该次只更新最终候选 Docker Codex/Claude 证据，不扩大 R5 的 RemoteWorker/Kubernetes 跨节点、deepseek 或完整故障矩阵结论。
- product-000096 已把 Workspace snapshot claim、cleanup claim 与 restore target 从仅 Docker 放宽到 Docker/Kubernetes；Control Plane 通过受管临时 Pod 和 Kubernetes `pods/exec` 使用同一 `portable-tar-v1` 摘要/归档语义，Helm 在启用 Worker 时提供持久 `/snapshots` PVC。未发布 `0.3.0-dev.178` 候选以最新三方配置完成真实 Kubernetes Codex 事件续读（两次各恢复 4 个 UID）和 Control Plane SIGKILL 后 `process-restart/recovered` attempt 2；本轮未执行 Kubernetes 跨节点快照接管，不能填充该故障格。日志 `.tmp/anywhere-runtime-r5-k8s-codex-r36.log` SHA-256 `sha256:e62a828b6e405ddc2d095ad3efbfa0a0badc88c03a80551f1cafba72dd841837`，Compose/Kubernetes 测试资源均精确清理为 0。
- product-000096 的 Kubernetes portable snapshot/restore 已在真实 OrbStack Kubernetes v1.35.6 完成：受管 PVC 上的 helper Pod 通过 v5 WebSocket `pods/exec` 导出并导入 10240-byte `portable-tar-v1` 归档，恢复后的摘要 `sha256:ee4e61a075419140f6ddf04e6150bb7595b5768f036934174ded8be4b72351bb` 与 proof 文件均核对一致，helper Pod、源/目标 PVC、隔离 namespace 和临时凭据目录均按归属精确清理。日志 `.tmp/anywhere-runtime-r5-k8s-portable-snapshot-r46-20260911.log` SHA-256 `sha256:be90d63ab8f1a9387c43069eafab04e909fa7571acb8128781edfc40fae07cea`；这是同一 Kubernetes target 的数据导出/恢复证据，不填充跨节点接管故障格。
- 同一候选的 Kubernetes 事件续读实测中，Codex、Claude Code 与 Pi 均从持久 `nextCursor` 断线后恢复到 `execution.complete`，每个 Provider 严格对账 4 个 UID；Codex 随后的 Kubernetes 进程重启恢复也通过。Claude Code 在 pending-side-effect 对账后第二次执行等待 600 秒仍返回 502，整轮按失败处理，Pi 的 Kubernetes 运行中恢复未因该失败继续执行；本轮资源仍精确清理为 0。失败日志（含已通过续读标记）`.tmp/anywhere-runtime-r5-compose-r35-updated-codex-claude-pi-k8s-events-20260911-r1.log` SHA-256 `sha256:42e336048235e5f40d4298893cb830302f713137a88436ebad445af74094df85`，不计为 Kubernetes 故障矩阵或 R5 完成证据。
- 在本机 kind `v0.33.0` / Kubernetes `v1.37.0` 双 worker 集群完成真实跨节点 Workspace 数据恢复：源 PVC 绑定 `ca-snapshot-source-pv`，helper Pod 运行在 `ca-anywhere-r5-k8s-worker`，通过 `pods/exec` 导出 10240-byte `portable-tar-v1` 归档（`sha256:6f29767cf7ac2b4464090d3e918d7d4601f5869137b7510caa0462a348d48d6c`）；随后删除源 namespace/PV 并物理移除源 worker 容器，目标 PVC 绑定 `ca-snapshot-destination-pv`、helper Pod 运行在 `ca-anywhere-r5-k8s-worker2`，只凭集群外归档恢复 proof 与相同摘要，RTO 186674 ms、RPO 0 byte。源节点状态已观测为 NotReady，目标恢复后 helper/PVC/归档清理为 0，kind 集群、节点容器、凭据目录均精确删除；证据 `.tmp/anywhere-runtime-r5-k8s-cross-node-evidence-20260911.json` SHA-256 `sha256:d9d1a66524b4044dc4e892569f351a1bd23a3db8123714d249dbcaeabdbb9f04`，源/目标日志 SHA-256 分别为 `sha256:8c143b37b129b155cfec651d8810ca9d7de6d9d748428582600dfce90309bd96` / `sha256:89b6ee5baf9b473ef1253db804bb450e8695f5d9de408fc57a5d434cc07f2894`。该实测关闭 Kubernetes portable snapshot 的跨节点数据恢复缺口，不等同于四 Provider 运行中 Turn 接管或 R5 完成。
- 同一候选的 outbound RemoteWorker 续读与恢复实测已通过：Codex、Claude Code 各自从持久 `nextCursor` 中断后严格恢复 4 个事件，Control Plane SIGKILL 后两者均以 `process-restart/recovered` 完成 attempt 2；同轮 Docker Claude 进程重启、Docker Codex 跨节点 `cross-node-takeover`（RTO 3305 ms、RPO 0 byte、快照 8125440 bytes）和 Docker 交互/80 秒长任务均通过，资源精确清理为 0。日志 `.tmp/anywhere-runtime-r5-compose-r35-updated-codex-claude-remote-events-20260911-r1.log` SHA-256 `sha256:a7d9b132f61f53aac0dbc58a78caf13179b0c46ec85b901ae9b4a82e3675b1bb`；这不替代 RemoteWorker 的跨节点接管或 Kubernetes/deepseek-harness 格子。
- 同一候选的 Pi outbound RemoteWorker 续读实测也通过：Docker 与 RemoteWorker 各从持久 `nextCursor` 中断后严格恢复 4 个事件，RemoteWorker 和 Docker 的运行中 Turn 均在对账后以 `process-restart/recovered` 完成 attempt 2；资源精确清理为 0。日志 `.tmp/anywhere-runtime-r5-compose-r35-updated-pi-remote-events-20260911-r1.log` SHA-256 `sha256:97a6fa99ab2344273b11572e02ff47b644872818dab6b0410733d2b16627ed30`；这不替代 RemoteWorker 跨节点接管或 Kubernetes/deepseek-harness 格子。
- 2026-09-11 在用户更新后的第三方凭据目录上修正 Durable Runtime 的恢复请求：只有非空 `providerResumeCursor` 才进入跨进程协议；仅有 checkpoint `resumeSnapshot` 时不再发送空 cursor，避免 deepseek-harness 将权威历史恢复误判为无效原生 cursor。脚本失败路径也会在精确清理前读回持久 Execution 错误，未输出 Secret。managedagent Go 测试、脚本语法和 `git diff --check` 通过。
- 未发布 dirty-source 候选 `0.3.0-dev.183`（22 项制品，checksums 全部通过）使用当前 `tenant-local.codex.json`/`tenant-local.claudeAgent.json` 三方 `https://hy.apiclaw.cc/v1` 配置，并以同目录 Pi/deepseek-harness 凭据完成 deepseek-harness Docker、outbound RemoteWorker、Kubernetes 的完整真实链路：新 Workspace/Sandbox、事件续读各 4 UID、工具文件副作用/Artifact/follow-up、Control Plane SIGKILL 后 `process-restart/recovered` 和精确清理均通过。日志 `.tmp/anywhere-runtime-r5-compose-r43-deepseek-docker-20260911.log` SHA-256 `sha256:a3256c119d3d1cfb21ec6bf64262fab0276b9a95c60204b0b1464d7242bab005`、`.tmp/anywhere-runtime-r5-remote-r49-deepseek-full-20260911.log` SHA-256 `sha256:6bd8c9c69d8a17ef0169864e8065f9dfc07c6ec03805f4c3903663b379c09d4d`、`.tmp/anywhere-runtime-r5-k8s-r48-deepseek-full-20260911.log` SHA-256 `sha256:ed916c6038ec245d24cd5fdf4444935446aff3cb58242e98f9af4f246a5134bf`。候选 Runtime、Deployment、Migrations、TypeScript SDK、Go SDK 摘要分别为 `sha256:7ca6b73478487c386752fee0288448584ead69b1cc8c67d33896ed11b72d4a74`、`sha256:b7535446f486c0910db66afd1f1fd514eefd8b3263f7d2f31cca655f366085eb`、`sha256:2b2502456d770b016e61bcac4d5d1fb9a85ba79f5eae5d6168e814786cbee8d9`、`sha256:a0cf7d497d33438c4c708c8f04aed2c2106b2751f35c5ffec3881e22346d5a50`、`sha256:6e1557b77ec0e11b2827dff596962376d3dbc2f0420f759fa7fb7e959986873b`；候选仅本机生成，未发布。
- 同一候选新增 Docker 跨节点 Provider 选择参数（默认仍为 Codex），并给跨节点实例使用独立 Session/Turn/Execution 标识，避免与普通恢复样例复用幂等 ID。四 Provider 均在真实 Docker 双 daemon 上完成源 writer fence、portable snapshot、目标 `docker-compose-target-restore` 接管和 attempt 2 `cross-node-takeover/recovered`，资源精确清理为 0；deepseek/Claude/Pi/Codex 日志 SHA-256 分别为 `sha256:997eaa0e07807ec14f730ad331857f93dc78341af92ef3de818f7693f29a162d`、`sha256:d13a861f15b7543d717aa9f8131901a843fe19899b667ede41e7548347776515`、`sha256:ae366c2d8bbbf4508b8983f402500e8edb1c028b070ef3cd72954ddc10ebdd7d`、`sha256:f9caf777966ae71d27386adb08bcbe1ba017613150da8b1cf717c3d56c851f8f`。这些结果关闭 Docker Provider 级跨节点样例，但不替代 RemoteWorker/Kubernetes 各 Provider 的跨节点 Agent Turn 接管。
- 修复 BASE RemoteWorker SSH 回归中的真实根因：Worker 已消费 OpenSandbox 的 `connected` 帧时，SSH bridge 不再重复等待该帧，而把成功的 exchange 作为连接屏障，避免 SSH shell 请求在 65 秒命令期限后失败。候选回归 `--remote-worker-only` 真实通过 RemoteWorker Sandbox Exec/Files/Preview/PTY/SSH、断连后的新尝试与精确清理；证据目录 `.tmp/anywhere-runtime-r5-base-regression-r64`，日志 SHA-256 为 `c2d3f9754036fc3b18d6124932213370aa401829254caaab02ed6bcdf6f88394`，证据 SHA-256 为 `9f775e5d0af900eb506f89ddc4c4d5d5f9e3c4e634f2b56d15dba67fe9800839`。
- 按 SSH bridge 修复重建未发布 dirty-source 候选 `0.3.0-dev.184`；22 项制品 `checksums.sha256` 全部通过。Runtime、Deployment、Migrations、TypeScript SDK、Go SDK 摘要分别为 `sha256:7ca6b73478487c386752fee0288448584ead69b1cc8c67d33896ed11b72d4a74`、`sha256:b7535446f486c0910db66afd1f1fd514eefd8b3263f7d2f31cca655f366085eb`、`sha256:2b2502456d770b016e61bcac4d5d1fb9a85ba79f5eae5d6168e814786cbee8d9`、`sha256:6ee5ae40f98c3b28bfb0ece89d8e51477d360beef4158e4f885c0b7bd75e7276`、`sha256:6e1557b77ec0e11b2827dff596962376d3dbc2f0420f759fa7fb7e959986873b`；候选仅本机生成，未发布。
- 当前开放边界：deepseek-harness Docker、outbound RemoteWorker、Kubernetes 的核心链路和进程重启恢复已在 `0.3.0-dev.184` 真实通过；四 Provider 的 Docker 跨节点接管和 Kubernetes 三节点跨节点 Agent Turn 接管已通过。RemoteWorker 的 Provider 级跨节点矩阵、Kubernetes 长任务/取消/交互等待、完整故障矩阵和相关 Admin 汇总仍未完成；受影响 BASE RemoteWorker 回归已由 r64 复测通过，R5 保持进行中。
- 2026-09-12 使用用户更新后的 `tenant-local.codex.json` 与 `tenant-local.claudeAgent.json`（第三方 `https://hy.apiclaw.cc/v1`，`gpt-5.6-luna`；不使用官方 API）重建未发布 dirty-source 候选 `0.3.0-dev.193`，22 项制品 `checksums.sha256` 全部通过。候选 Runtime、Deployment、Migrations、TypeScript SDK、Go SDK 摘要分别为 `sha256:7ca6b73478487c386752fee0288448584ead69b1cc8c67d33896ed11b72d4a74`、`sha256:b7535446f486c0910db66afd1f1fd514eefd8b3263f7d2f31cca655f366085eb`、`sha256:a201978129998316e806e530353096a3a67afcaa9d8199f93626683485465cc4`、`sha256:5a35b7aa56baa4320b7e1a6c7a55b8a72cfe7494aada18bcefee897c4e2c54df`、`sha256:6e1557b77ec0e11b2827dff596962376d3dbc2f0420f759fa7fb7e959986873b`；候选仅本机生成，未发布。
- 同一候选完成真实三节点 kind Kubernetes 的 Codex 跨节点运行中接管：浏览器 smoke、事件续读各 4 个 UID、Control Plane SIGKILL 后旧 Sandbox 停止并释放 writer，源 namespace 与 worker 节点被实际删除，11,264,000-byte `portable-tar-v1` 快照在 `worker2` 恢复，目标 Sandbox/Workspace 运行并读回原文件，未知副作用经摘要核对后 attempt 2 `cross-node-takeover/recovered`；RTO 34,483 ms、RPO 0 byte。恢复目标 cleanup preview/cleanup、Docker 与 Kubernetes 资源精确清理均通过，整轮脚本 exit 0。日志 `.tmp/anywhere-runtime-r5-k8s-cross-node-r86.log` SHA-256 `sha256:29f47c5cdf29bc560f587543211db02b3052191887a8b9ac32ce5c68b65114ee`。为使该闭环可重复，Kubernetes Worker 列表只筛选 Worker label，运行时目标凭据补齐 deployment descriptor，kind 验收 Role 对齐已有 target 准备权限；RemoteWorker/Kubernetes 其他 Provider 跨节点格、完整故障矩阵和 R5 总体验收仍保持开放。
- 同一候选完成真实三节点 kind Kubernetes 的 Pi 跨节点运行中接管：旧 writer fence、源 namespace 与 worker 删除、`portable-tar-v1` 快照在 `worker2` 恢复、目标 Sandbox/Workspace 读回原文件，未知副作用经摘要核对后 attempt 2 `cross-node-takeover/recovered`；RTO 48,631 ms、RPO 0 byte、快照 61,440 bytes，脚本 exit 0。日志 `.tmp/anywhere-runtime-r5-k8s-cross-node-pi-r90.log` SHA-256 `sha256:d795660ff90b738087622b6fc6f50e907fb793222a8bcfec498b990d3278958d`。
- 同一候选完成真实三节点 kind Kubernetes 的 Claude Code 跨节点运行中接管：旧 writer fence、源 namespace 与 worker 删除、`portable-tar-v1` 快照在 `worker2` 恢复、目标 Sandbox/Workspace 读回原文件，未知副作用经摘要核对后 attempt 2 `cross-node-takeover/recovered`；RTO 101,626 ms、RPO 0 byte、快照 665,600 bytes，脚本 exit 0。日志 `.tmp/anywhere-runtime-r5-k8s-cross-node-claude-r91.log` SHA-256 `sha256:6688a6f69cdd8730df1149945be6f553017ff3f08f68823e7f28aed2e54a1b2a`。
- 同一候选的 deepseek-harness Kubernetes 首次真实复跑中，Control Plane SIGKILL 后原节点 `process-restart/recovered` 已通过；跨节点实例在产生 `pending-side-effect` checkpoint 前结束，脚本按安全条件停止，未 fence/接管或盲目重放。该次未通过记录保留，日志 `.tmp/anywhere-runtime-r5-k8s-cross-node-deepseek-r92.log` SHA-256 `sha256:1e0acd2bc0285c2d17113656353c2065b8ded8d204e929b034ad43d61ec6d5df`，kind 与 Compose 资源已精确清理。
- 同一候选的 deepseek-harness Kubernetes 复跑在跳过已单独验证的进程重启样例后完成真实三节点跨节点运行中接管：旧 writer fence、源 namespace 与 worker 删除、`portable-tar-v1` 快照在 `worker2` 恢复、目标 Sandbox/Workspace 读回原文件，未知副作用经摘要核对后 attempt 2 `cross-node-takeover/recovered`；RTO 64,232 ms、RPO 0 byte、快照 1,771,520 bytes，脚本 exit 0。结合前次同候选进程重启 marker，Kubernetes deepseek-harness 两类恢复均有真实证据；日志 `.tmp/anywhere-runtime-r5-k8s-cross-node-deepseek-r93.log` SHA-256 `sha256:45353e1fd97c594d8ed912887dc9eda776c88a52f6aa86d85fcdc91a48042413`，kind 与 Compose 资源已精确清理。
- 2026-09-12 使用更新后的 Codex/Claude Code 凭据重跑未发布 dirty-source 候选 `0.3.0-dev.193`：第三方网关 `https://hy.apiclaw.cc/v1`、模型 `gpt-5.6-luna`，未调用官方 OpenAI/Anthropic API；凭据文件保持 `0600`，副本与本机源文件 SHA-256 均为 `sha256:fd860e13d3d1bf2592cbda1c716c5908c64777754410fb6efd864ca37986aa43`。真实 Compose smoke exit 0；Codex/Claude outbound RemoteWorker 的运行中 Control Plane 重启恢复均为 attempt 2 `process-restart/recovered`（同一 RemoteWorker target，副作用摘要已确认），Codex Docker 跨节点接管为 RTO 3,296 ms、RPO 0、快照 8,144,896 bytes，真实长任务 81 s、Approval/User Input 均通过；报告 `.tmp/anywhere-runtime-r5-compose-third-party-20260912-r94.log` SHA-256 `sha256:5fa01557a5bf75510c6690b94f47d018edee7e236eacc396dc3e01f9af606ea9`，本轮 Compose 资源已清理。该轮不宣称 RemoteWorker Provider 级跨节点快照/恢复，相关矩阵仍开放。
- 同一第三方凭据重跑暴露并修复 RemoteWorker 长 PTY 期间的 mTLS 轮换竞态：轮换改为先完成当前证书 heartbeat、命令结算和本轮命令处理，空闲后再切换身份；新增 RemoteWorker 状态测试。代码修复与状态测试通过；随后真实 Compose r98 exit 0，Codex/Claude outbound RemoteWorker 进程重启恢复、Docker Codex 跨节点接管（RTO 3,284 ms、RPO 0、快照 8,209,408 bytes）、Docker 82 s 长任务、RemoteWorker 81 s 长任务、Approval/User Input/Cancel/Interrupt 均通过，未再出现 `REMOTE_WORKER_SANDBOX_PTY_RECEIPT_CONFLICT`；报告 `.tmp/anywhere-runtime-r5-compose-third-party-20260912-r98.log` SHA-256 `sha256:fa1558e6aa187ec31bfc8181b9a1b0ee69fe6ba1073a0b9027b4efc1afbe2c0d`，Compose 资源精确清理为 0。该轮仍不宣称 RemoteWorker Provider 级跨节点快照/恢复或 Kubernetes 长任务/交互故障格。
- r98 后重建未发布 dirty-source 候选 `0.3.0-dev.194`，包含 RemoteWorker 轮换竞态修复与交互脚本的 Foundation 直接绑定参数修正；22 项制品 `checksums.sha256` 全部通过。Runtime、Deployment、Migrations、TypeScript SDK、Go SDK 摘要分别为 `sha256:7ca6b73478487c386752fee0288448584ead69b1cc8c67d33896ed11b72d4a74`、`sha256:4e7909eefaef5a85b9e6e8b948f2798d9f12d913ef334c3ed27ce7acae4a7f12`、`sha256:a201978129998316e806e530353096a3a67afcaa9d8199f93626683485465cc4`、`sha256:9487a916c17007452a36b3d38c789537138904ede3ec24b60f0a1f80734dd7fa`、`sha256:6e1557b77ec0e11b2827dff596962376d3dbc2f0420f759fa7fb7e959986873b`；候选目录 `.tmp/anywhere-runtime-r5-20260912-r98-final`，仅本机生成，未发布。
- 使用候选 `0.3.0-dev.194` 和同一第三方配置的 Kubernetes 重跑保留失败边界：r100 在 Claude Code 进程重启恢复后执行保持 `recovering/claim_expired` 并超时，未宣称通过；r101 的进程重启恢复成功，但跨节点样例在产生 `pending-side-effect` checkpoint 前结束，脚本按安全条件停止，未 fence/接管或重放。两轮 kind/Compose 资源均精确清理，日志 SHA-256 分别为 `sha256:2c6b3edc409b91fb0bd0fbf8a15146729d7c65577bd0284e84a04d8e68b47e0e` / `sha256:fe48225e700b05993b826efd9936330513da769e7eda5dd8a01560b53dbd3f64`；Kubernetes 长任务/取消/交互故障格与 RemoteWorker Provider 级跨节点仍保持开放。
- 针对 r104 真实 Kubernetes Sandbox 在 512Mi 限制下被 OOMKilled，Kubernetes Runtime、Foundation、restore profile 的测试内存上调为 1Gi；同时删除跨节点验证前重复执行同节点恢复的循环，避免非目标 Provider 阻断后续 fence/接管。未发布 dirty-source 候选 `0.3.0-dev.197`（22 项制品，`checksums.sha256` 全部通过）使用同一第三方 `https://hy.apiclaw.cc/v1` 配置；Runtime、Deployment、Migrations、TypeScript SDK、Go SDK 摘要分别为 `sha256:7ca6b73478487c386752fee0288448584ead69b1cc8c67d33896ed11b72d4a74`、`sha256:4e7909eefaef5a85b9e6e8b948f2798d9f12d913ef334c3ed27ce7acae4a7f12`、`sha256:a201978129998316e806e530353096a3a67afcaa9d8199f93626683485465cc4`、`sha256:6c11587a68fd14edd6ca242b3b41b540b7b36ea5994bf9e01f5028f531d4ba90`、`sha256:6e1557b77ec0e11b2827dff596962376d3dbc2f0420f759fa7fb7e959986873b`；候选目录 `.tmp/anywhere-runtime-r5-20260912-r109-final`，仅本机生成，未发布。
- 候选 `0.3.0-dev.197` 的真实 Kubernetes 双 worker 验收已补齐：Codex 跨节点运行中接管 `cross-node-takeover/recovered`（RTO 33,205 ms、RPO 0、快照 7,321,600 bytes，日志 `.tmp/anywhere-runtime-r5-compose-k8s-codex-third-party-20260912-r106.log` SHA-256 `sha256:4ba94c8d7c236aff101ba5b4cdd5c4edfb36c2a15119f9db0d0e9c33d794f89e`）；Claude Code 跨节点运行中接管 `cross-node-takeover/recovered`（RTO 66,272 ms、RPO 0、快照 4,812,800 bytes，日志 `.tmp/anywhere-runtime-r5-compose-k8s-claude-third-party-20260912-r109.log` SHA-256 `sha256:28d0ef01e751dffaad9072d4c4216e48f511d745a433a3f4539cad96ad006e68`）。完整 r110 真实 Compose/Kubernetes smoke exit 0，Codex/Claude 事件续读、同节点进程重启恢复、Claude 跨节点接管、81/80 秒长任务、Approval/User Input、浏览器 smoke 和精确资源清理均通过；日志 `.tmp/anywhere-runtime-r5-compose-k8s-third-party-20260912-r110.log` SHA-256 `sha256:c445d197aecc2eb64e168a5351b865356b5d78d5e7c4363b54b0c241c9f1aec5`。以上均为第三方网关实测，不调用官方 OpenAI/Anthropic API；RemoteWorker Provider 级跨节点和完整四 Provider 故障矩阵仍保持开放。
- 同日的仓外 SDK 复核再次通过：`bun scripts/test-platform-sdk-consumers.ts` 在隔离临时目录完成精确 TypeScript/Go 制品安装、Fresh TypeScript 编译、`GOWORK=off` Fresh Go 编译及真实 loopback Control Plane/Worker 调用；TypeScript artifact `sha256:4abd47e966daa6f6768b5f13fbc85b9da97771ac0847111441ddc708f4fada4b`、Go module zip `sha256:adfaddf7dd8da8e91fcbbe04020bfe4650cffdd87310cf0d1530ecf02fecaa0a`，临时目录清理完成。
- 全量 `GOTOOLCHAIN=local GOFLAGS=-mod=readonly go -C services/control-plane test ./...` 的非 R5 基线边界已记录：除 `internal/migration` 外其余 control-plane 包通过；该包仍因现有 quota fixture 期望 11 条而当前 manifest 有 13 条、sealed session internals spread 检查和 `TestRunnerLedgerEntrySuccessPostCommitStateAndRegistryTamperRevokesCursor` 达到 10 分钟上限而失败。未修改这些无关 dirty 基线测试；R5 相关 Go 包、Provider/SDK/Admin 测试与生成检查均单独通过。
- 2026-09-12 再次直接核对用户更新的 `provider-credentials/tenant-local.codex.json` 与 `tenant-local.claudeAgent.json`：两份均为 `0600`、配置摘要 `sha256:fd860e13d3d1bf2592cbda1c716c5908c64777754410fb6efd864ca37986aa43`，第三方 endpoint 为 `https://hy.apiclaw.cc/v1`、模型为 `gpt-5.6-luna`；Codex `/responses` 与 Claude `/messages` 使用该配置均返回 HTTP 200，未访问官方 API。该配置验证不改变 RemoteWorker Provider 级跨节点与完整故障矩阵仍开放的结论。
- 2026-09-12 使用同一更新后的三方 Codex/Claude 配置重跑未发布 dirty-source 候选 `0.3.0-dev.197`：真实 Admin/User 浏览器 smoke、四次事件续读（每次严格恢复 4 个 UID）、Codex/Claude outbound RemoteWorker `process-restart/recovered` attempt 2、Codex Docker `cross-node-takeover`（RTO 3,313 ms、RPO 0、快照 7,645,696 bytes）、Docker 81 s/RemoteWorker 83 s 长任务以及 Approval/User Input 均通过；脚本 exit 0，Compose 容器/卷/网络精确清理为 0。日志 `.tmp/anywhere-runtime-r5-compose-admin-timeout-20260912.log` SHA-256 `sha256:ec3e8c6c530aad8d507186a11202d102dbe3d85ae3d3c8cef3e38b76ff4b715d`。为避免 DevTools 无界等待，Admin Web 测试新增 5 秒 WebSocket 打开超时；`node --check` 与 `git diff --check` 通过。该轮仍不宣称 RemoteWorker Provider 级跨节点快照/恢复、Kubernetes 长任务/交互故障格或 R5 总体验收。
- 针对 RemoteWorker 快照真实运行暴露的两个根因完成最小修复：RemoteWorker 快照回执查询改为 tenant-bound mutation，避免 global mutation 缺失 `cloud_agents.tenant_id`；product-000097 覆盖共享 snapshot reaper 的含 OUT 参数 SQL 列歧义，并把 `workspace-snapshot` 纳入数据库能力校验。Worker heartbeat 现在会在每轮处理挂起的 Workspace Snapshot command；mTLS 快照上传/下载、目标身份校验和 TS/Go 生成契约保持一致。迁移/SDK 生成检查、SQL 分类测试和相关 Go 测试通过。
- 未发布 dirty-source 候选 `0.3.0-dev.201` 使用用户更新后的 `tenant-local.codex.json`/`tenant-local.claudeAgent.json` 三方 `https://hy.apiclaw.cc/v1`、`gpt-5.6-luna`（凭据保持 0600，未调用官方 API）；Runtime/Control Plane/Deployment/Migrations/TypeScript SDK/Go SDK 摘要分别为 `sha256:7ca6b73478487c386752fee0288448584ead69b1cc8c67d33896ed11b72d4a74`、`sha256:5988f5b85c8966983484599b21bd0f79b4fae7ef38975751ee30744b5932f557`、`sha256:2f1187797d56fe8873338120100f74c67b19b2359a025cebbb742ae565d3ea12`、`sha256:9f5d6402656473327d76efe3b345c68e28114d9b843e593660303712890b2051`、`sha256:935954489b2e2b5b14a2e24ebe85fbe346b00c56ac1d5c236b2b72a5950a4f8d`、`sha256:3ee1b822a2854d4adcb1dd572dbea018e7d3c95b9bd0092400fa2b4933a3535a`；候选目录 `.tmp/anywhere-runtime-r5-20260912-r114-remote-worker`，manifest SHA-256 `sha256:ca9714084076684e248ed8aea6f62c6acee7e0c9999a1da7de33a61d6fe81632`，仅本机生成、未发布。
- 候选 `0.3.0-dev.201` 的真实 Compose RemoteWorker 核心断言已通过：Admin/User 浏览器 smoke、4 次事件续读（每次 4 个 UID）、Codex/Claude outbound RemoteWorker Control Plane SIGKILL 后 attempt 2 `process-restart/recovered`、Codex 源/目标不同 RemoteWorker 的 writer fence→portable-tar-v1→源卷/源 OpenSandbox 删除→目标 Workspace/Sandbox 恢复及 Session/CLI 重绑定均通过；跨节点 `RTO 12,849 ms`、`RPO 0`、快照 `12,297,728 bytes`。同轮 Docker/RemoteWorker 81/82 秒真实长任务、Approval/User Input 通过；但脚本最终清理因共享源执行节点上的基线 Foundation Sandbox 未先停止而失败，随后 trap 已精确清理资源，因此该轮不计作 exit 0 验收。日志 `.tmp/anywhere-runtime-r5-remote-worker-20260912-r114.log` SHA-256 `sha256:bd2e535c5b04db29904d6a77216ab588b13b12fada138c3893363e602563ba09`。测试编排现已在物理移除共享源节点前停止并释放该 Sandbox writer，待真实重跑确认；Claude/Pi/deepseek-harness 的 RemoteWorker Provider 级跨节点矩阵、Kubernetes 长任务/取消/交互等待、完整故障矩阵和 R5 总体验收仍保持开放。
- 测试编排修复后，候选 `0.3.0-dev.201` 的四 Provider outbound RemoteWorker 跨节点矩阵均真实 exit 0：Claude Code `cross-node-takeover/recovered`（RTO 10,850 ms、RPO 0、快照 641,536 bytes，日志 `.tmp/anywhere-runtime-r5-remote-worker-20260912-r117-claude.log` SHA-256 `sha256:787fcd3fbcbeb4aca7d3d1c4a2bec55d396168d665550ecd2c14e9add7a9d5e8`）；Pi（RTO 10,848 ms、RPO 0、快照 40,448 bytes，日志 `.tmp/anywhere-runtime-r5-remote-worker-20260912-r118-pi.log` SHA-256 `sha256:06b09e792d1786336d07990cfc0dda25d4f2afed32f059b0955a45208c861a61`）；deepseek-harness（RTO 9,740 ms、RPO 0、快照 1,712,128 bytes，日志 `.tmp/anywhere-runtime-r5-remote-worker-20260912-r119-deepseek.log` SHA-256 `sha256:bc0eabe567fa319aeb66d893c59fc80487c26b92a9c4d3a2db6288f3048f79cd`）；Codex（RTO 11,935 ms、RPO 0、快照 7,305,728 bytes，日志 `.tmp/anywhere-runtime-r5-remote-worker-20260912-r120-codex.log` SHA-256 `sha256:60bd4d79ef89ccfff8f5f62bb09f571ecf1c73a5ab8e7b1a7de38cfcf12d24dc`）。四轮均同时包含同节点 process-restart attempt 2、Admin/User 浏览器 smoke、4 次事件续读、真实源/目标不同、writer fence、portable-tar-v1、源卷/源 OpenSandbox 删除、目标 Workspace/Sandbox 恢复与精确清理；均使用用户更新的第三方 Provider 配置，未访问官方 API。
- 同一候选在当前 OrbStack Kubernetes 完成 Docker＋Kubernetes 联合复跑：Codex/Claude Code 的 4 次事件断连续读、两环境 `process-restart/recovered`、80/79 秒真实长任务、Cancel/Interrupt、Approval/User Input、Admin/User 浏览器与最终清理均通过，日志 `.tmp/anywhere-runtime-r5-docker-k8s-20260912-r123-codex-claude.log` SHA-256 `sha256:4a951c4718cd465a1c2cf854ef1ff8ec7ae0bca2cea0ad1f4c8dbb9b91c2faff`；Pi/deepseek-harness 的同环境事件续读与进程重启恢复也通过，日志 `.tmp/anywhere-runtime-r5-docker-k8s-20260912-r124-pi-deepseek.log` SHA-256 `sha256:37e7a5d71406045b592fc7e8b947b7c37d672c25499d551067ca2b5335f94506`。两轮均 exit 0，使用更新后的第三方 Provider 配置，未访问官方 API。
- 候选 `0.3.0-dev.201` 又在逐 Provider 重建的三节点 kind Kubernetes 中完成四个运行中 Turn 的原节点物理移除与跨节点接管：Codex RTO 47,185 ms、RPO 0、快照 7,331,840 bytes（`.tmp/anywhere-runtime-r5-k8s-20260912-r127-codex.log`，`sha256:581f3d26cd52664de14002e68b41f76dcd693346e57ad5884423e5a84359ae4c`）；Claude Code 47,669 ms、0、675,840 bytes（`.tmp/anywhere-runtime-r5-k8s-20260912-r129-claude.log`，`sha256:990570cbcd6df6c2e462b06f81110ed3e836efe9707d52f996366c1c5ec89a57`）；Pi 95,364 ms、0、61,440 bytes（`.tmp/anywhere-runtime-r5-k8s-20260912-r130-pi.log`，`sha256:8740ecefc1f24ba7264888f102a9b2cadaa7fa4ab4c397a57feda81389207153`）；deepseek-harness 49,405 ms、0、1,771,520 bytes（`.tmp/anywhere-runtime-r5-k8s-20260912-r131-deepseek.log`，`sha256:f44c43946c47ff7a769677bc99969f025ab6f3102579e2bffa008a76957dd91c`）。四轮均先 fence 并停止旧 writer，再删除源 namespace/PV 与源 worker，使用集群外 portable archive 在 worker2 恢复 Workspace/Sandbox、核对摘要与未知副作用、由 attempt 2 `cross-node-takeover/recovered` 完成；脚本 exit 0 且测试资源精确清理。
- 在同一候选与更新后的第三方配置上，RemoteWorker 四 Provider 的完整核心格也逐个 exit 0：Codex RTO 8,614 ms、快照 11,117,056 bytes（`.tmp/anywhere-runtime-r5-remote-worker-20260912-r132-codex-core.log`，`sha256:4cfc4d1d9d91073d65110828da2fb6b41d4214dca9859171241fc4a7414372e1`）；Claude Code RTO 8,643 ms、1,117,184 bytes（`.tmp/anywhere-runtime-r5-remote-worker-20260912-r133-claude-core.log`，`sha256:ce4abdd138ed956317263791ce36184958f35461bbc27268e1de319fbf873421`）；Pi RTO 8,626 ms、54,784 bytes（`.tmp/anywhere-runtime-r5-remote-worker-20260912-r134-pi-core.log`，`sha256:a4a3b3e502645680d84a9ea535da4b2733042c41763e34d344747a6a30596eb7`）；deepseek-harness RTO 8,686 ms、2,479,104 bytes（重跑 `.tmp/anywhere-runtime-r5-remote-worker-20260912-r137-deepseek-core-rerun.log`，`sha256:4b88a4dbf91f378f61e35c25739e18a177bdc241dcb7b3694e660bd1d2c38f7e`）。每轮均包含真实 Workspace/Sandbox→Session→Turn→文件 Artifact→事件续读→同 Workspace follow-up、同节点 `process-restart/recovered`、旧 writer fence、源节点/卷删除、目标恢复和精确清理；首次 deepseek 重跑的清理竞态失败日志 `.tmp/anywhere-runtime-r5-remote-worker-20260912-r135-deepseek-core.log` 保留但不计入。
- 当前候选的仓外 SDK 复跑 exit 0：隔离 Fresh TypeScript 与 `GOWORK=off` Fresh Go consumer 均从精确候选制品安装、编译并完成真实 loopback Control Plane/Worker 调用；TypeScript artifact `sha256:a6774fac8d84fceab7b84d7c67a99ed94d65b06c13fa64066408558dc92d6bbe`，Go module zip `sha256:c114805c452ee6485baf780e82473637f81bc5696094c32946ab8a3b743b94c4`，日志 `.tmp/anywhere-runtime-r5-platform-sdk-consumers-20260912-r121.log` SHA-256 `sha256:082bf5f607a008ae2f61949dc16f2ace32d14e81f311206d25986da58f6ec80b`，临时 consumer 已清理。
- 2026-09-12 针对交互等待期间的进程重启完成最小安全接管修复：Control Plane 在过期 claim 的 pending interaction 上先通过既有 Runtime Open/Close fence 旧 writer，再返回 `awaiting_reconciliation`；Provider 仅在恢复消息含权威原始请求时跳过失效 native cursor，并对同一 generation 的 approval/user-input 做一次性精确消费，`confirmed` 副作用禁止重放；Worker writer lease 按 `tenantID + executionID` 隔离。新增 managedagent/Worker fence 测试，Control Plane/Worker/Provider 测试、`sh -n` 与 `git diff --check` 通过。
- 使用用户更新的两份 `0600` 三方配置（`https://hy.apiclaw.cc/v1`、`gpt-5.6-luna`，配置摘要 `sha256:fd860e13d3d1bf2592cbda1c716c5908c64777754410fb6efd864ca37986aa43`，未调用官方 OpenAI/Anthropic API）生成未发布 dirty-source 候选 `0.3.0-dev.204`，22 项制品校验通过；候选目录 `.tmp/anywhere-runtime-r5-20260912-r150-interaction-candidate`，Runtime/Deployment/Migrations/TypeScript SDK/Go SDK 摘要分别为 `sha256:7c9c690e187f9530aed3ce598391cd5c9f48b7360dba92eaadaef6daa416d894`、`sha256:589896686abf7a5ad585b5e55b8a212a7b45b0ec22b2354c286d49e4cb48a827`、`sha256:9f5d6402656473327d76efe3b345c68e28114d9b843e593660303712890b2051`、`sha256:3aab61182d1bc15ea09c05ae7e426a9af63db7a22a2257e147621b55343971a1`、`sha256:3ee1b822a2854d4adcb1dd572dbea018e7d3c95b9bd0092400fa2b4933a3535a`；manifest SHA-256 `sha256:9aabc767811c7cf5b6668a7bcac5d2f414a23556dbd96867f9005af32e12e82f`，仅本机生成、未发布。
- 同一候选完成真实 Docker Compose exit 0：Admin/User 浏览器 smoke（18/6 请求、普通用户 Admin 403）、两次事件流断连续读各恢复 4 个 UID、Codex Docker `process-restart/recovered`、Codex/Claude approval 与 user-input 在 Control Plane SIGKILL 后均先 fence/对账再 attempt 2 恢复、真实长任务 80 s、Cancel/Interrupt 均通过；最终输出 `platform Compose smoke passed`，本轮 containers/networks/volumes 均为 0。父验收脚本在交互重启后刷新动态 Control Plane 端口，避免旧映射导致清理阶段误报。日志 `.tmp/anywhere-runtime-r5-compose-interaction-20260912-r151.log` SHA-256 `sha256:429c4ffc7fdd4fdbb621f6863b123ca7a05d1f33c2760e8a9ae58164ea4db571`。
- 2026-09-12 完成公共仓外 SDK 的真实在线闭环修正：TypeScript/Go 消费者均复用精确候选制品，通过第三方 `https://hy.apiclaw.cc/v1` 的真实 Docker Control Plane 完成 Workspace/Sandbox 绑定、Session/Turn、幂等 Turn、事件游标续读和取消；TypeScript 另完成 Approval、Workspace 文件 Artifact 字节下载，取消时同步 Execute 的 HTTP 499/`CANCELLED` 被按契约核对。Go 在线消费者固定使用本地 module proxy/cache，避免退回 Git 远端。候选 `0.3.0-dev.209` 目录 `.tmp/anywhere-runtime-r5-20260912-r167-final-candidate`，source dirty、仅本机未发布；Runtime/Deployment/Migrations/TypeScript SDK/Go SDK/manifest 摘要分别为 `sha256:7c9c690e187f9530aed3ce598391cd5c9f48b7360dba92eaadaef6daa416d894`、`sha256:872d1be73f8bac5e74fd3cc35962d928d418c1d623f49714d3f118b642b63d3f`、`sha256:9f5d6402656473327d76efe3b345c68e28114d9b843e593660303712890b2051`、`sha256:2961fcf1b42e95a9e21894cd39b23b037367c436bc9b4e1a6de7f486ead3cbdd`、`sha256:3ee1b822a2854d4adcb1dd572dbea018e7d3c95b9bd0092400fa2b4933a3535a`、`sha256:c7ebc436e7e264c936def9eb93eea8d7ca7f610ac5f9d21f188b1b67df3ff85a`；在线日志 `.tmp/anywhere-runtime-r5-compose-sdk-live-20260912-r168.log` SHA-256 `sha256:c7b941b85e86f519ea05e5a5bdfc4bde118ca0eecceafd8d98e03f1a3d911637`，包含 `live-typescript-consumer-ok` 与 `live-go-consumer-ok`，最终临时资源 containers/networks/volumes 均为 0。
- 同一候选完成双 Provider Admin Runtime 真实 Docker 验收 exit 0：Codex/Claude 的事件续读各恢复 4 个 UID，Control Plane SIGKILL 后 Approval/User Input 均先 fence/对账再恢复，真实长任务 83 s、Cancel/Interrupt 通过；Admin/User 浏览器 smoke 为 21/6 请求，普通用户调用 Admin API 为 403，Admin 页面从真实 `/v1/admin` 数据渲染 `Anywhere Runtime`，未暴露用户内容/Secret，双语/主题/视口检查通过。日志 `.tmp/anywhere-runtime-r5-compose-admin-runtime-20260912-r169.log` SHA-256 `sha256:f9005cb337ec82c57206636a64d979d385f9f28eba7cf0d0b3d623e535a5652d`，最终临时资源 containers/networks/volumes 均为 0。
- 缺失 portable snapshot 现在在归档读取边界直接映射为 `ErrDeploymentConflict`，避免控制器对已不存在的数据进行无界重试；新增单元覆盖 missing/corrupt，相关 Docker target、managedagent/worker、生成检查与脚本语法均通过。基于当前 dirty source 重新生成未发布候选 `0.3.0-dev.210`（目录 `.tmp/anywhere-runtime-r5-20260912-r174-snapshot-negative-candidate`，22 项制品、manifest SHA-256 `sha256:eccc24b53c64931753cba8b58988d44b782e2321fde7c6fd9147cc4a6a32c66f`）；Runtime/Deployment/Migrations/TypeScript SDK/Go SDK 摘要分别为 `sha256:7c9c690e187f9530aed3ce598391cd5c9f48b7360dba92eaadaef6daa416d894`、`sha256:872d1be73f8bac5e74fd3cc35962d928d418c1d623f49714d3f118b642b63d3f`、`sha256:9f5d6402656473327d76efe3b345c68e28114d9b843e593660303712890b2051`、`sha256:94e02246c5a41ebe10bc6baa828341d5b5bfd5d72afef22f9cdc21590d527088`、`sha256:3ee1b822a2854d4adcb1dd572dbea018e7d3c95b9bd0092400fa2b4933a3535a`；候选仅本机生成、未发布。
- 2026-09-12 共享 portable snapshot 负向路径完成一次真实物理核对：删除源 Docker-in-Docker daemon 与本地卷后，OrbStack 目标在不接触源节点的情况下分别拒绝缺失归档和损坏归档，随后用恢复的原归档完成精确内容恢复；RTO `2758 ms`、RPO `0`、目标卷/辅助容器/源节点/归档最终清理为 0。负向输出 `.tmp/anywhere-runtime-r5-20260912-r172-snapshot-negative/negative.log` SHA-256 `sha256:070cd849f917f376825174f51f3cf1eaa71af315a173e7713da3bb756f338470`，完整证据 `.tmp/anywhere-runtime-r5-20260912-r172-snapshot-negative/evidence.json` SHA-256 `sha256:391d60f257d46fad393166fdb1620c05da3de8a53b2bdba9cf7cb9c7fe7e0527`，运行日志 `.tmp/anywhere-runtime-r5-cross-node-snapshot-negative-20260912-r173.log` SHA-256 `sha256:8e382e8af7ea5643b097678040c25275b2adb80959bbb9da4525c2897077bca8`。该条覆盖共享 snapshot 机制的 missing/corrupt 负向，不外推到尚未实测的 Provider/环境组合。
- 2026-09-13 修正 Worker Runtime wire 的进程退出语义：`runtime_execution_failed` 现在映射为可识别的 `ErrRuntimeProcessUnavailable`，只有已有持久 checkpoint 时才保留 `running`，普通 Runtime/Provider 错误仍按失败处理；新增生成 wire 与 durable execution 测试，`go test ./internal/workerclient ./internal/managedagent`、`go vet`、`sh -n` 和 `git diff --check` 通过。
- 基于当前 dirty source 生成未发布候选 `0.3.0-dev.214`，22 项制品校验通过；候选目录 `.tmp/anywhere-runtime-r5-20260913-r192-agent-exit-candidate`，manifest SHA-256 `sha256:d458de5e3e1b85a65ccb65cd3c8ef2db168df3e80207cfceebbe87993552b3d0`，checksums SHA-256 `sha256:034389e2d0358ad922c9d17f58c1d2180fc04aab8febdfa41ec6777bd407578e`；Runtime/Deployment/Migrations/TypeScript SDK/Go SDK 摘要分别为 `sha256:7c9c690e187f9530aed3ce598391cd5c9f48b7360dba92eaadaef6daa416d894`、`sha256:2f28ef6d892842e78adac88ce041e0c971bd758f131e5ed1686bc13763c707df`、`sha256:9f5d6402656473327d76efe3b345c68e28114d9b843e593660303712890b2051`、`sha256:9b24b5f02115a39053b7072f80cdc08683ae26792f9822b5e245a98747a32b62`、`sha256:3ee1b822a2854d4adcb1dd572dbea018e7d3c95b9bd0092400fa2b4933a3535a`；候选仅本机生成、未发布。
- 同一候选使用用户更新的两份 `0600` 三方 Codex/Claude 配置完成真实 Docker Compose exit 0：控制面 SIGKILL 后 `process-restart/recovered`，双 Provider Approval/User Input 等待期间重启恢复，真实长任务 `79 s`，Worker 进程退出保留 attempt 1 checkpoint 并完成 `worker_exit_survival=passed`；Agent Runtime 进程被精确 SIGKILL 后先保持 checkpointed `running`，旧 writer fence 后以 attempt 2 `same-node-reconnect/recovered` 完成 `agent_process_exit_recovery=passed`，无盲目重放。Admin/User 浏览器 smoke 为 21/6 请求、普通用户 Admin 403，Admin Runtime 为 true；最终输出 `platform Compose smoke passed`，containers/volumes/networks/OpenSandbox resources 均为 0。日志 `.tmp/anywhere-runtime-r5-compose-agent-exit-20260913-r196.log` SHA-256 `sha256:073da21ae6b2d168e36204696101087823be09a57b8e76e8248f22a3f29ef4b2`。
- 为补齐 Gateway 负向门禁，候选 `0.3.0-dev.217`（目录 `.tmp/anywhere-runtime-r5-20260913-r204-expired-grant-candidate`，manifest SHA-256 `sha256:f9618dde1edfd679a216dd82c1864a1bd7cfd3b22d92303dc337744687767add`，checksums SHA-256 `sha256:4e17748026ecfdf1a8585411ed544eae12f87218f45d6f0a317e364375d76c79`）在真实 Compose 中创建合法最短 60 秒 Grant，实际等待过期后用 Gateway Files 请求得到 403，并由 Admin 列表观察到 `expired`；同轮 Codex 事件续读、Control Plane SIGKILL 后 attempt 2 `process-restart/recovered`、备份恢复和精确清理均通过，日志 `.tmp/anywhere-runtime-r5-compose-expired-grant-codex-20260913-r206.log` SHA-256 `sha256:d7e15ca98b5a18c319a9d1c4f292e32cb95c18ba59eea99a9284398963e30518`。四 Provider 同范围复跑的过期 Grant 标记也已通过，但后续 Claude 交互受三方上游一次性 `RUNTIME_FAILED` 中止，退出码 1，不计为整轮通过。
- PostgreSQL Execution 投影读取边界新增 checkpoint 负向锁定：running Execution 的 transcript 缺失、消息摘要损坏或 checkpoint protocol 版本不兼容均返回 `ErrCoordinationResultDrift`，不会退回空历史继续恢复；`go test ./internal/store/postgres -run 'TestScanManagedAgentExecution(RejectsUnusableCheckpoint|RestoresRuntimeTranscript)'` 通过。该单元证据只锁定 fail-closed 行为，真实故障注入矩阵仍保持开放。
- 使用候选 `0.3.0-dev.217` 与当前四份三方凭据完成 RemoteWorker 真实完整回归并 exit 0：四 Provider 事件续读各恢复 4 个 UID；四 Provider 控制面 SIGKILL 后均以 attempt 2 `process-restart/recovered`，Codex 另在不同 RemoteWorker 上先 fence 旧 writer、恢复 portable snapshot 后以 `cross-node-takeover/recovered` 接管（RTO `8,668 ms`、RPO `0`）；Docker/RemoteWorker 长任务分别 `79/82 s`，Approval/User Input、取消/中断与 Admin/User 浏览器隔离均通过。日志 `.tmp/anywhere-runtime-r5-remote-worker-latest-20260913.log` SHA-256 `sha256:6ca2f92648406a6dbfa58a55ffa3e0c7c3f0a1899c963c6026cb9b7d32fdec8d`，最终 containers/volumes/networks 为 0；本轮继续使用 `https://hy.apiclaw.cc/v1`，未访问官方 OpenAI/Anthropic API。
- 在交互脚本加入旧 generation 的真实拒绝断言后，重新生成候选 `0.3.0-dev.218`（目录 `.tmp/anywhere-runtime-r5-20260913-r218-stale-interaction-candidate`，manifest SHA-256 `sha256:c3eb0a90353e9a0c5efda62ea13b9b3f044fa059a0ec8d4d9ef2cdd9a2bb2671`，checksums SHA-256 `sha256:3990aaa68da9098346ed420fabca5d3ec7ccacf320b3c17859221c1eba8d47e4`，22 项制品校验通过）。四 Provider Docker 真实 Compose exit 0：事件续读各恢复 4 个 UID，过期 Grant 403、Approval/User Input 旧代次拒绝、Control Plane SIGKILL 后恢复、79 秒长任务、Worker/Agent 进程退出恢复、取消/中断、Admin/User 浏览器隔离和备份恢复均通过；日志 `.tmp/anywhere-runtime-r5-compose-r218-four-provider-20260913.log` SHA-256 `sha256:71e61cabfba2d9a808ae9f3250df84fabb5917adc02d3fb6d67546be1b86e776`，最终 containers/volumes/networks 为 0。
- 修正真实 PostgreSQL 故障注入脚本改用 stdin 执行 `psql`（`-c` 不展开 psql 变量）后，生成未发布候选 `0.3.0-dev.221`（目录 `.tmp/anywhere-runtime-r5-20260913-r221-checkpoint-negative-candidate`，manifest SHA-256 `sha256:4142fca4322b524c96763c849d149fda9f866c4dd84a6f0e0ab8c8ab28e416a5`，checksums SHA-256 `sha256:567dacda14c3d7ccac10fb9fc6968b9c2e44037a14c8ff6fcf3a100294f1b1fd`，22 项制品校验通过）。当前三方 `hy.apiclaw.cc/v1` 配置下，Codex/Claude Code Docker Compose 真实 exit 0：checkpoint protocol 改为 `runtime-message-checkpoint-v0` 后 `execution get` fail-closed，旧 generation Approval/User Input 拒绝、控制面重启恢复、80 秒长任务、Worker/Agent 进程退出恢复、取消/中断、Admin/User 隔离和备份恢复均通过；日志 `.tmp/anywhere-runtime-r5-compose-r221-checkpoint-negative-retry-20260913.log` SHA-256 `sha256:a1bdeb45754fd0fb8940f9e74067728a2668bc6bb789d365f3ac724d95acbe3a`，最终 containers/volumes/networks 为 0。该轮确认了 checkpoint protocol 的真实负向路径，未调用官方 OpenAI/Anthropic API。
- 为使跨节点测试在共享 OpenSandbox 已被前一接管切片回收后仍可精确清理，将 Compose 恢复脚本的源容器删除改为幂等；生成未发布候选 `0.3.0-dev.222`（目录 `.tmp/anywhere-runtime-r5-20260913-r222-remote-worker-candidate`，manifest SHA-256 `sha256:0d70a6de6c3f122bbb9003a378bb790f182226b17818959d2c1f0715e9001e70`，checksums SHA-256 `sha256:166502fb283729267e598c6cec149001754a1dbf160e2924e3f6056a35a12ed7`，22 项制品校验通过）。使用当前四份三方配置完成四 Provider Docker + outbound RemoteWorker 真实回归并 exit 0：八次事件续读各 4 个 UID，四 Provider RemoteWorker 控制面 SIGKILL 后均 attempt 2 `process-restart/recovered`；Codex 在不同 RemoteWorker 上先 fence 旧 writer、恢复 `portable-tar-v1` 快照后 `cross-node-takeover/recovered`（RTO `10,609 ms`、RPO `0`、快照 `13,865,984 bytes`）；Docker/RemoteWorker 长任务 `80/82 s`，checkpoint protocol 与旧 generation 负向、Approval/User Input、Worker/Agent 退出、取消/中断、Admin/User 隔离和备份恢复均通过。日志 `.tmp/anywhere-runtime-r5-remote-worker-r222-20260913.log` SHA-256 `sha256:d58dc73a05f9d65f2d7ba83a7eebc063ce4bf662e7e885c7dfbc18d042e8d017`，最终 containers/volumes/networks 为 0；全程未访问官方 OpenAI/Anthropic API。
- 候选 `0.3.0-dev.223`（目录 `.tmp/anywhere-runtime-r5-20260913-r223-checkpoint-transcript-candidate`，manifest SHA-256 `sha256:20517af9d08a5bbcaf3647b9c59bf4f191004384bdd307b8266833fa3b083aa5`，checksums SHA-256 `sha256:3bd01dc78b027b1b87e2d3efb4d84b5400c5a51cae1c8503f5f9a83e4be7f37a`，22 项制品校验通过）新增真实 checkpoint transcript 缺失/摘要损坏注入；在当前三方配置的 Codex/Claude Code Docker Compose exit 0 回归中，`checkpoint_protocol_negative=passed` 与 `checkpoint_transcript_negative=passed`，旧 generation、控制面重启恢复、81 秒长任务、Worker/Agent 退出、取消/中断、Admin/User 隔离和备份恢复均通过。日志 `.tmp/anywhere-runtime-r5-compose-r223-checkpoint-transcript-20260913.log` SHA-256 `sha256:77f8751c98dfd81f311de10aa6307a806c9688cae8f3715b870090d702d5762b`，最终 containers/volumes/networks 为 0；未调用官方 OpenAI/Anthropic API。
- 候选 `0.3.0-dev.225`（目录 `.tmp/anywhere-runtime-r5-20260913-r225-snapshot-version-candidate`，manifest SHA-256 `sha256:d7999cfa851181fb41cebf3e0a935fbb8f01a346990b80d92212be9adc2efbee`，checksums SHA-256 `sha256:3bb4d89a2addc3eccab3b91678a227a25507722333c0d205bce2d910cf8957cf`，22 项制品校验通过）补齐真实 snapshot backend 版本负向：将已可用 portable snapshot 暂时改为不兼容的 `docker-volume-v1`，带合法幂等请求执行 restore，服务返回 409 `RESOURCE_CONFLICT` 后恢复原 backend；同轮 Codex Docker 跨节点旧 writer fence/接管为 `cross-node-takeover/recovered`（RTO `3,295 ms`、RPO `0`、快照 `7,683,584 bytes`），事件续读各 4 UID、过期 Grant 403、旧 generation Approval/User Input 拒绝、checkpoint protocol/transcript、控制面重启、81 秒长任务、Worker/Agent 退出、取消/中断、Admin/User 隔离和备份恢复均通过。日志 `.tmp/anywhere-runtime-r5-compose-r225-snapshot-version-cross-node-20260913.log` SHA-256 `sha256:df083fabec6adb5b1e545370b09e08d2cd860fe109b1c52c79529288956114f8`，最终 containers/volumes/networks/OpenSandbox resources 为 0；Runtime/Deployment/Migrations/TypeScript SDK/Go SDK 摘要分别为 `sha256:7c9c690e187f9530aed3ce598391cd5c9f48b7360dba92eaadaef6daa416d894`、`sha256:4137b15f26510a0fcec8073e32a3d3ce2d4a71c2fa24af6ae2fee67babf06e1b`、`sha256:9f5d6402656473327d76efe3b345c68e28114d9b843e593660303712890b2051`、`sha256:0e6b2f4d399738452c14745fae965fb35feeebfcf3c1ee7970c054d68b28940f`、`sha256:3ee1b822a2854d4adcb1dd572dbea018e7d3c95b9bd0092400fa2b4933a3535a`；使用 `https://hy.apiclaw.cc/v1` 三方配置，未调用官方 OpenAI/Anthropic API。
- 候选 `0.3.0-dev.226`（目录 `.tmp/anywhere-runtime-r5-20260913-r226-cross-tenant-candidate`，manifest SHA-256 `sha256:b87c4b9d0278ab0a8a8ab59a9c430ffca1b1701ed52905f713499294167e7d34`，checksums SHA-256 `sha256:0785004a76f0ef3e3f3675624df26eb24b9fcd3247f2c919b357e5437c6831a0`，22 项制品校验通过）在当前三方配置的 Codex/Claude Code Docker Compose exit 0 回归中，pending Approval 与 User Input 以另一租户路径回答均真实返回 401 `authentication_failed`；旧 generation 仍拒绝，事件续读、控制面重启、80 秒长任务、Worker/Agent 进程退出、取消/中断、Admin/User 隔离和备份恢复均通过。日志 `.tmp/anywhere-runtime-r5-compose-r226-cross-tenant-20260913.log` SHA-256 `sha256:7dcbfd46c00b1f683334556f6fd8d8fb4824b446a40842d0313ae520fb86abc5`，最终 containers/volumes/networks 为 0；未调用官方 OpenAI/Anthropic API。
- 候选 `0.3.0-dev.228`（目录 `.tmp/anywhere-runtime-r5-20260913-r228-auth-negative-candidate`，manifest SHA-256 `sha256:e0547f16fa4ef908eec56bdaae2722844655d064c5b3c4074d93278023f9c7c4`，checksums SHA-256 `sha256:4c1c0169792f3ea8a5e4dfd36d654501f8f1bbd7a41195075ba1091fa9425696`，22 项制品校验通过）在当前三方配置的 Codex/Claude Code Docker Compose exit 0 回归中，pending Approval 与 User Input 分别用超出时钟偏差窗口的过期 JWT、推进 `securityEpoch` 后的旧 token 做回答，均真实返回 401；信任快照以更高 generation 恢复后同一交互继续完成。跨租户/旧 generation 拒绝、checkpoint protocol/transcript、事件续读、控制面重启、79 秒长任务、Worker/Agent 进程退出、取消/中断、Admin/User 隔离和备份恢复均通过。日志 `.tmp/anywhere-runtime-r5-compose-r228-auth-negative-20260913.log` SHA-256 `sha256:a8aa34095b38df84daa5f8c737280e770b1087ae87a89cffd4aaa765cedca37b`，最终 containers/volumes/networks 为 0；未调用官方 OpenAI/Anthropic API。
- 候选 `0.3.0-dev.238`（目录 `.tmp/anywhere-runtime-r5-20260913-r238-worker-exit-candidate`，manifest SHA-256 `sha256:760593b6a5347d7fa1109a14d50358af302b9ded8f214d8932cbe2e0fc200029`，checksums SHA-256 `sha256:6092f10199ff60ba86f2917ea57a11540bde5f5a06c92082ed92b4ec7bd5d15a`，22 项制品校验通过）修正 Worker 进程退出后允许“原 claim 续跑 attempt 1”或“租约过期后对账接管 attempt 2（必须 recovered）”的真实断言；使用当前四份三方配置中的 Codex/Claude Code 完成 Docker 与 outbound RemoteWorker 真实 Compose exit 0。Docker/RemoteWorker 均通过事件续读（各 4 UID）、过期 Grant、跨租户/旧 generation、Approval/User Input 授权过期/撤销、checkpoint protocol/transcript 负向、控制面 SIGKILL 后 `process-restart/recovered`、80/85 秒长任务、Worker 退出保留 checkpoint、Agent Runtime 退出安全接管、取消/中断、Admin/User 隔离和备份恢复；RemoteWorker Codex 另在不同节点完成 writer fence、`portable-tar-v1` snapshot、snapshot backend 版本负向和 `cross-node-takeover/recovered`，RTO `8,653 ms`、RPO `0`、快照 `27,632,640 bytes`。最终输出 `platform Compose smoke passed`，containers/volumes/networks 为 0；日志 `.tmp/anywhere-runtime-r5-compose-r238-worker-exit-20260913.log` SHA-256 `sha256:e8a801ae91455e41adac26d0b5ad16764d14011e69da43545c66cbb6d777ef59`，全程使用 `https://hy.apiclaw.cc/v1`，未调用官方 OpenAI/Anthropic API。
- 2026-09-13 复核当前源凭据文件（均为 `0600`，不输出 Secret）直接访问三方 `https://hy.apiclaw.cc/v1/models`：Codex 与 Claude Code 均返回 HTTP `200`，模型均为 `gpt-5.6-luna`；本次探针未访问官方 API。
- 2026-09-13 在临时 kind `v0.33.0` / Kubernetes `v1.37.0` 三节点集群以候选 `0.3.0-dev.238` 运行四 Provider 真实 Kubernetes Compose：Codex、Claude Code、Pi、deepseek-harness 的 Control Plane SIGKILL 后 `process-restart/recovered` 均通过，Approval 负向与控制面重启等待也通过；随后 Claude approval 接管的执行请求在 300 秒后仍为 `running/recovering`、attempt 2，未取得成功终态，故本轮不计为 Kubernetes 交互/长任务矩阵通过。失败日志 `.tmp/anywhere-runtime-r5-k8s-r238-four-provider-20260913-r2.log` SHA-256 `sha256:012741203f527ca8d46f605fc23052d32045c1e8c633ae71986f4eaa8e235f30`；测试 namespace、RBAC、Pod、Docker Compose 资源及 kind 集群已精确清理为 0。
- 2026-09-13 修正运行中接管的实际 fence 边界：OpenSandbox 的 `takeover=1` 只会把同一 PTY 重新附着到新请求，不能使旧 Provider Runtime 停止；Foundation Runtime 现按稳定 `executionID` 在 Sandbox 内写入受保护 PID 文件，打开新 writer 前核验 `/proc/$pid/cmdline` 并精确 SIGKILL 旧 `cloud-agent-runtime`，退出时只清理当前 owner。旧三参数测试调用保持兼容；`go test ./internal/managedagent` 通过。该修复把同 Sandbox 重连从“重新连接”变为可审计的旧 writer fence，未知副作用仍只按 checkpoint/对账处理。
- 2026-09-13 针对真实 outbound RemoteWorker 的 `request_remote_worker_sandbox_file_v1` 与 heartbeat 并发 `40P01`，将文件 command 事务改为 `READ COMMITTED` 下最多三次、50/100 ms、可取消的死锁重试；新增 PostgreSQL 单元断言。此前 r239 的真实文件读取曾因该死锁映射为 502 `ARTIFACT_UNAVAILABLE`，本修复不吞掉其他错误；`go test ./internal/store/postgres ./cmd/cloud-agents-remote-worker` 与 `go vet` 通过。
- 候选 `0.3.0-dev.240`（目录 `.tmp/anywhere-runtime-r5-20260913-r240-remote-deadlock-candidate`，manifest SHA-256 `sha256:65755e01b29107153ac358ff53062d6a50cc7e70e5aa4d8341f6bbe6f70162da`，checksums SHA-256 `sha256:734e203a86499fdcd22baa8084b18b19da45402e7e77e2aaeed2221ab34b0d07`，22 项制品校验通过）在当前三方配置下完成 Codex outbound RemoteWorker 真实 Compose exit 0：事件续读、Control Plane `process-restart/recovered`、snapshot backend 版本负向、旧 writer fence 后 `cross-node-takeover/recovered`（RTO `8,631 ms`、RPO `0`、快照 `11,111,424 bytes`）和最终精确清理均通过；日志 `.tmp/anywhere-runtime-r5-compose-r240-codex-remote-rerun.log` SHA-256 `sha256:51cd9f5c9565b35ece830202f5b27fe5e76f4810e9bed3fdeac8692359f59db5`。
- 候选 `0.3.0-dev.241`（目录 `.tmp/anywhere-runtime-r5-20260913-r241-remote-ready-candidate`，源提交 `b7df543ce34c257852763429f9056fd2e6877de9`、source dirty、manifest SHA-256 `sha256:165354cd6b0af7de7e71c47d74fc1667c0528e0e73fa7d1ef07709c1bc23e52b`、checksums SHA-256 `sha256:ec42552338a07f4df0dae59a90cbb3d13c24d55248b1aff14e1616e953fa645d`，22 项制品校验通过）使用当前四份三方凭据完成四 Provider Docker + outbound RemoteWorker 回归：Docker 的事件续读、交互等待重启、80 秒长任务、Worker/Agent 退出和 Admin/User smoke 均通过；RemoteWorker 四 Provider Control Plane 重启均 `process-restart/recovered`，Approval/User Input 交互重启与 `83 s` 长任务通过。该轮在 RemoteWorker Worker 退出场景于交互出现前真实变为 `failed`（`execution became failed before interaction`），脚本 exit 1，不计为完整四 Provider RemoteWorker 通过；日志 `.tmp/anywhere-runtime-r5-compose-r241-four-provider-remote.log` SHA-256 `sha256:cef5c72e45e5dffc53fdedcc6ebee08f4ffa6afbdc8ce381edf36165c2ef6cbd`，containers/volumes/networks 最终均为 0。独立 Codex/Claude 聚焦轮亦由一次上游 `RUNTIME_FAILED` 中止，未把该轮标为通过。
- 使用同一候选 `0.3.0-dev.241` 与当前四份三方凭据的 r243 真实回归中，Docker 四 Provider 的事件续读、交互授权与 Control Plane 重启、80 秒长任务、Worker/Agent 退出和 Admin/User smoke 全部通过；RemoteWorker 四 Provider 的 Control Plane `process-restart/recovered`、Approval/User Input 交互重启与 `82 s` 长任务也通过。RemoteWorker Worker 退出场景仍在交互出现前真实进入 `failed`（`execution became failed before interaction`），脚本 exit 1；不重放未知副作用，不计为完整故障矩阵通过。日志 `.tmp/anywhere-runtime-r5-compose-r243-four-provider-remote.log` SHA-256 `sha256:eac9f19956cccc0f8f2f719b6320c0dba415b5ef96a2c26ac40d3075fc7235f9`，Compose containers/volumes/networks 最终均为 0。
- 同一候选与三方凭据的 r244 聚焦 Codex/Claude Code 真实 Compose exit 0 通过：Docker 与 outbound RemoteWorker 的事件续读、交互授权负向、checkpoint 负向、Control Plane 重启、80/83 秒长任务、Docker Worker/Agent 退出、RemoteWorker Worker 退出和 Admin/User smoke 均通过；RemoteWorker Codex 跨节点 `cross-node-takeover/recovered` 的 RTO 为 `9,727 ms`、RPO `0`、快照 `26,884,096 bytes`。日志 `.tmp/anywhere-runtime-r5-compose-r244-codex-claude-remote.log` SHA-256 `sha256:daf387845e4952f3c3e35c92353e5071c09aeaa4f01e7b572c904dc0af96303c`，Compose containers/volumes/networks 最终均为 0。
- r245 四 Provider 真实回归在 Docker User Input 交互续跑时遇到 OpenSandbox 真实 WebSocket proxy `ProtocolError: invalid status code`，Control Plane 返回 `502 runtime_failed`，未将未知副作用重放或计作通过；日志 `.tmp/anywhere-runtime-r5-compose-r245-four-provider-remote.log` SHA-256 `sha256:4f874b532ead3dcb05faa90a0e2bd3d2bb238cbe555bff2954bf4a275fba75b0`，Compose containers/volumes/networks 最终均为 0。该轮同时观测到 RemoteWorker heartbeat 与文件 command 的 PostgreSQL serialization/deadlock，现有文件事务最多三次重试未吞掉其他错误。
- r246 使用同一候选与三方凭据完成 Pi/deepseek-harness Docker＋outbound RemoteWorker exit 0：两 Provider 事件续读各恢复 4 个 UID，控制面 SIGKILL 后均 `process-restart/recovered`，snapshot backend 版本负向通过；Pi 在不同 RemoteWorker 上完成 writer fence、`portable-tar-v1` 恢复与 `cross-node-takeover/recovered`，RTO `9,753 ms`、RPO `0`、快照 `1,687,552 bytes`。日志 `.tmp/anywhere-runtime-r5-compose-r246-pi-deepseek-remote.log` SHA-256 `sha256:f91ffd368eeadfbc17f85d45dc7142c65edcce3f5b4b1fdb86ba7f344ed9a7c0`，Compose containers/volumes/networks 最终均为 0。
- r247 使用同一候选与三方凭据再次完成 Pi/deepseek-harness Docker＋outbound RemoteWorker exit 0：Pi 与 deepseek-harness 的事件续读、控制面 `process-restart/recovered`、snapshot backend 版本负向均通过；deepseek-harness 在不同 RemoteWorker 上完成旧 writer fence、portable snapshot 和 `cross-node-takeover/recovered`，RTO `8,611 ms`、RPO `0`、快照 `2,511,360 bytes`。日志 `.tmp/anywhere-runtime-r5-compose-r247-deepseek-remote.log` SHA-256 `sha256:cf8fbddb14c26b793817a8ea05988cbfcdec7bb1b985ad4a31b8e6e1596b0326`，Compose containers/volumes/networks 最终均为 0。
- r251 使用同一候选与当前四份三方凭据；测试编排将 Agent Sandbox TTL 延长至 `7200 s`，用户测试 token 保持 profile 允许的 `3500 s` 生命周期。Docker 四 Provider 的事件续读、交互、80 秒长任务、Worker/Agent 退出及 Admin/User smoke 均通过；RemoteWorker 四 Provider 的控制面重启、Approval/User Input 交互重启与 auth/checkpoint 负向均通过。随后 RemoteWorker 组合在真实 OpenSandbox PTY WebSocket proxy 遇到 `websockets.exceptions.ProtocolError: invalid status code`，Control Plane 返回 `runtime_turn_failed`（recovered attempt 2）；未盲目重放未知副作用，不计为稳定四 Provider 组合通过。日志 `.tmp/anywhere-runtime-r5-compose-r251-four-provider-remote.log` SHA-256 `sha256:e389926506c5a6c9c8800811dd6e427703e4b6b0b02871d9a8509d2f3006afff`，Compose containers/volumes/networks 最终均为 0。
- 上述 r245/r251 的 OpenSandbox proxy 瞬态故障与交互未达 checkpoint 属历史未通过轮次；后续验证保留其日志和安全边界，不把未知副作用重放为成功。
- 候选 `0.3.0-dev.249`（目录 `.tmp/anywhere-runtime-r5-20260913-r267-recorded-input-candidate`，22 项制品）纳入稳定的 user-input `requestId`/问题精确匹配和 Worker runtime 失败诊断；manifest SHA-256 `0328b027b71333fe8a2e413bc3a9ab914e3c603df3a029ada2b2754c7a8731b6`，checksums SHA-256 `c9ffd4c6a2e4c91f17137222264663caecd979d28ce201d1baa7bab703d5d1f8`，`sha256sum -c` 通过，仅本机生成、未发布。
- 该候选的 Docker/RemoteWorker 四 Provider 聚合回归 exit 0：事件续读每 Provider 各恢复 4 个 UID，Approval/User Input 过期、撤销、跨租户和旧 generation 均拒绝，checkpoint protocol/transcript、控制面重启等待、80/82 秒长任务、Worker/Agent 退出、取消/中断及 Admin/User 浏览器 smoke 均通过。Docker+RemoteWorker 日志 `.tmp/anywhere-runtime-r5-compose-r268-recorded-input.log` SHA-256 `8ed6df15f648bd0161b41a2fc9d1944ed1033dbe5350c69a85af129a2f748fee`、`.tmp/anywhere-runtime-r5-compose-r269-four-provider-remote.log` SHA-256 `e3e1af9fad842ed0bc4fc45a8f6ac6336947ceed401c1f006d12e185b3991a51`；最终 Compose containers/volumes/networks 为 0。
- 同一候选的仓外 SDK 真实验收通过：隔离目录精确安装候选后，TypeScript 与 `GOWORK=off` Go consumer 均完成真实 Session/Turn、事件续读、恢复、Approval/Artifact/Cancel 调用；日志 `.tmp/anywhere-runtime-r5-compose-r270-sdk-live.log` SHA-256 `80cd88d9751da9517235d07e7fd765047f92c9382d9c2c7fdbd6b04563e146cd`。
- 三节点 kind `v0.33.0` / Kubernetes `v1.37.0` 在固定 OpenSandbox v0.2.0 CRD/RBAC/controller 前置后，候选四 Provider 的 Kubernetes 事件续读、进程重启恢复、Approval/User Input 负向、checkpoint protocol/transcript、控制面重启等待、80 秒长任务、Worker 退出、Agent 交互和 Codex 跨节点接管均通过；Codex 跨节点 RTO `32919 ms`、RPO `0`、快照 `29050880 bytes`。日志 `.tmp/anywhere-runtime-r5-k8s-20260913-r273-four-provider.log` SHA-256 `3d1fe0dd1f7db3cc67fdc6bcfa53f249fa8c0e97084549839f7c5a743b3fe2b`，kind 集群、namespace、RBAC、PVC/Pod 和 Compose 资源均清零。
- 候选四 Provider 的 provider-specific 跨节点接管已覆盖三环境并全部 `cross-node-takeover/recovered`、RPO `0`：RemoteWorker Claude/Pi/deepseek 日志分别为 `.tmp/anywhere-runtime-r5-remote-20260913-r274-claude-cross-node.log`（RTO `10785 ms`，SHA-256 `2787406b56f135c9e7c995b13d9c03d81342d3d61e0eeec9c0a09ee658ecbd7c`）、r275 Pi（RTO `8643 ms`，SHA-256 `23b0d685fc4f1a64a5355a4a144257cffce054238d75dee2f93c92e380db3b12`）、r276 deepseek-harness（RTO `8638 ms`，SHA-256 `0bdb874b2621a105d2d9c82eceba5cf97fcc8b030aa0cc69342c5c0019cbcac5`）；Kubernetes Claude/Pi/deepseek 日志分别为 r277（RTO `65023 ms`，SHA-256 `a6d4bac98e99d6b43ffde0e7ab7d44d0f5cabff22135daef72e707f32c26c58b`）、r278（RTO `95249 ms`，SHA-256 `2eec7eff8873f12bda16d1aa9fff644450badc813c788038cca3063aadf35be9`）、r279（RTO `47140 ms`，SHA-256 `2dfd590354d50f797c373072fb842bdebe68ca8b5a68ca48ac84356c9a1e3aca`）；Docker Codex/Claude/Pi/deepseek 日志分别为 r280（RTO `3316 ms`，SHA-256 `4ee3cb1bf8a165d31b8013515785bd4436c3fef56afba9c6e8c31ec0d1dfaa1d`）、r281（RTO `3319 ms`，SHA-256 `4d65ef65c6762fda3d844a2caebf820e41ee5a122aa4ce4bcf4264872e4c3367`）、r282（RTO `3301 ms`，SHA-256 `08cca71bfeedc9aef6c3bbdbdedb45828415cd476ba5d6d57e0224313d89da45`）、r283（RTO `3284 ms`，SHA-256 `2d26e714e8aa62dc41583f3f5d7a82439248ee823c6a96b9771cf4b72fd50945`）。每轮均先释放旧 writer/fence，再恢复 portable snapshot 到新 Target；kind/RemoteWorker/Docker 测试资源均精确清理。
- R5 完成条件现已满足：四 Provider × Docker/outbound RemoteWorker/Kubernetes 的真实 Workspace/Sandbox 与统一 Runtime/SDK 闭环、运行中 Turn 持久认领/checkpoint/对账/fenced takeover、事件断线、控制面/Worker/Agent/节点故障、交互授权和 snapshot 负向、长任务、Admin 验收及仓外 TS/Go 调用均有当前候选证据。`ANYWHERE-RUNTIME-V1` 标记为 VERIFIED；完整用户对话 UI 继续留在 U1。完整 `platform:contracts:check`/SDK 生成检查仅因仓库 pin 的 Bun/uv/Go 版本与本机版本不一致而停止，未以构建通过替代真实验收。

### 本轮文档检查（2026-09-09）

- 已更新 01～07、三个 README 入口与架构 HTML，共 11 份文档；04 保存唯一实施切片与新 Goal，06 保存当前缺口与下一项。
- Python 标准库静态检查通过：222 个本地链接、69 个锚点、HTML 标签嵌套和 ID 唯一性；修复了 07 指向旧 Goal 的失效引用。
- BASE-READY 十二条、BASE-ADMIN-V1 十三条与旧正式 Gate 正文同本轮编辑前逐字比对一致；已有 04/06 编码约束补充和无关 tracked dirty diff 保留，`git diff --check` 通过。
- 浏览器策略拒绝本地 `file://` 预览，未进行视觉验收；没有用静态结构检查替代渲染或运行验收。本轮未运行 Provider/E2E、迁移或部署，也未提交、发布或启动新 Goal。

### BASE 实施与验收记录

- 2026-09-05 用户已明确把本任务从原 ADMIN-M1～M4 迁移到 BASE-M0～M5，授权当前仓库实现、相关验证与本地提交；采用固定 BASE-READY / BASE-ADMIN-V1。文档基线 `cde5bdb07117526af4fc4595d94b43a6a8ab7880`；不修改其他任务，不把旧 ADMIN-WEB-V1 未完成项标为通过。
- BASE-M0 已开始。固定 OpenSandbox server `v0.2.2` / source `207d94c7`、execd `v1.0.21` 和镜像 digest；真实 OrbStack Docker 29.4.0 linux/arm64 PoC 通过无 Provider 的 create/exec/Files、删除计算保留卷与重建读回摘要、缺失卷拒绝及精确清理。见 [候选核对与领域映射](evidence/base-m0-opensandbox-candidate-20260905.md)。
- 候选缺口已实证：相同创建输入产生两个 Sandbox；不存在的入口程序初始被接受为 Running，随后变为 Failed/exit 127。不能用创建响应替代 CP 幂等、Workspace 单写和就绪判断。当前仅技术 PoC，不是产品后端或 BASE-M0 联合验收。
- Go 运行时回执接缝已新增：`internal/opensandbox` 按完整归属/generation/摘要发现已有物理对象，重复或冲突回执 fail closed，清理前复核回执；标准 HTTP 限制重定向、响应大小和错误内容。固定 Docker 实测 14 项含 Go 发现、重复拒绝、旧 generation 拒绝及清理重放，卷/Files 摘要保留；见 [Go 接缝证据](evidence/base-m0-go-receipts-20260905.md)。该接缝现已接入下述持久化 Controller；原 PoC 仍只证明其固定技术边界。
- 接入前修复公共 ID 兼容缺口：128 字符/含 `~` 的合法 Identifier 采用 v2 双半 SHA-256 标签，发现与清理仍核验完整归属，不缩短公共契约。真实候选 14 项通过，见 [ID 回执兼容证据](evidence/base-m0-public-id-receipts-20260905.md)。未接通产品持久化链；新编码不承担旧 PoC 对象迁移。
- 首条持久化内核已落地：新 foundation profile 生成 Go/SQL 身份，000053 用独立 Workspace/Volume/Sandbox 表与既有 Operation/outbox/finalizer/Audit 原子接受意图。PostgreSQL 17.6 实测并发重放、冲突回滚、RLS、重启持久化和独占认领；Go 绑定完整解析后摘要。见 [内核证据与未覆盖项](evidence/base-foundation-intents-20260905.md)。SQL/数据库 fixture 不代表产品链路完成。
- 000053 已纳入版本化产品安装链：真实 runner 从 product-000052 精确升级一条到 000053，当时产品 CLI 全新安装 53 条并在重放时 no-op；两条路径均核对账本和新表。该冻结包现作为 000054 的升级输入保留。见 [产品迁移证据](evidence/base-foundation-product-migration-20260905.md)。未构建/发布镜像或部署；本机 `uv` 与仓库 pin 不符，因此不声称 umbrella contract suite 通过。
- 独立 no-Agent RuntimeProfile 与首条公开 Sandbox admission 已接通：product-000054 增加不可变 draft/published/disabled authority，生成 SDK、Admin/User HTTP 与 project/product scope 校验；User 只提交 profile ID/version，公开投影不含 Target/image/release/endpoint/凭据引用。PostgreSQL 17.6 使用真实签名 token 和生成客户端验证生命周期、普通用户 Admin 403、disable/replay、RLS 和 durable Operation/outbox；见 [API 与 authority 证据](evidence/base-runtime-profile-api-20260905.md)。该初始证据使用 ready Target SQL fixture；物理执行与 Admin 页面由下一条补齐。
- product-000055 已把 claim/renew/settle/reaper 接入生产启动的 Controller：真实 OrbStack Docker 中创建长期卷和 OpenSandbox，在首进程结算前退出后由第二进程回收 claim、严格 adopt 同一 runtime，并保留相同 Workspace 摘要；真实 Failed runtime 精确补偿，最终无 test-owned Sandbox/volume。生成 Admin Sandbox list/detail、服务端普通用户 403 和敏感字段排除通过；Admin Web 已提供真实 RuntimeProfile 与 Sandbox/Workspace 列表、详情及 Profile 生命周期，八种双语/主题/视口组合无溢出。见 [Controller 与 Admin 联合证据](evidence/base-m1-controller-admin-20260906.md)。本轮是本地 Docker/一次性 PostgreSQL 与本地 Web，不是部署、Kubernetes 或客户节点证据。
- 手动 Stop/Rebuild 垂直切片已接通：product-000056 以 generation/resourceVersion/精确资源确认接受持久化 Operation/outbox/Audit；真实 OpenSandbox Stop 删除计算并释放 writer、同一物理卷 Rebuild 后文件摘要不变，旧 generation 和外来卷 owner 被拒绝，普通用户 Admin API 为 403。Admin Web 用生成 SDK 和显式影响确认提交真实请求，202 只显示“请求已接受”。见 [Sandbox 生命周期证据](evidence/base-m1-sandbox-lifecycle-20260906.md)。本轮仍是本地 Docker/一次性 PostgreSQL 与本地 Web，不是部署、Kubernetes 或客户节点证据。
- TTL 自动停止垂直切片已接通：product-000057 用数据库时钟计算/判断 `expiresAt`，Controller 原子接受标准 Stop Operation，不另建物理删除路径；旧记录不回填 TTL，Rebuild 刷新期限。真实等待 60 秒后 generation 4 自动 Stop 删除计算并保留卷，generation 5 Rebuild 读回同一文件摘要；Admin API/Web 展示 TTL、到期时间与 manual/ttl 原因，Audit 和精确清理通过。见 [TTL 与恢复证据](evidence/base-m1-sandbox-ttl-20260906.md)。结合既有 Controller/Admin 和手动生命周期证据，BASE-M1 已覆盖当前固定退出范围。
- BASE-M2 首个同步 Exec 垂直切片已接通：Product API 只接受 Sandbox generation、bounded command 和 timeout；Control Plane 经服务端 user scope、PostgreSQL project authority 和完整物理回执执行固定 `/workspace` 前台命令，生成 Go/TypeScript SDK 与 CLI 同步更新。真实 OpenSandbox 返回 Workspace 摘要、stderr 和 exit 7；Admin 403、旧 generation 409、超 1 MiB 413，响应不含 endpoint/runtime/credential。见 [同步 Exec 证据](evidence/base-m2-sandbox-exec-20260906.md)。这不是 PTY/Files/访问网关或 BASE-M2 阶段完成。
- BASE-M2 PTY 垂直切片已接通：product-000058 持久化短期 Grant、PTY session 映射和签发/撤销 activity；独立固定路由 Access Gateway 逐请求及活跃连接重验 generation/expiry/revoke authority。真实 PTY 在 `/workspace` 执行，Gateway 重启与 cursor 重连无丢失，超过 1.1 MiB 输出时回放固定为 1 MiB；错误 token、跨 tenant、过期/撤销均拒绝，撤销关闭活跃连接。Admin API/Web 只显示 Grant 元数据并带 fencing/确认撤销，不返回 token、终端/文件内容或凭据。见 [PTY 与 Gateway 证据](evidence/base-m2-sandbox-pty-20260906.md)。本切片未做 Admin 浏览器视觉矩阵，不表示 BASE-M2 完成。
- BASE-M2 Files 垂直切片已接通：product-000059 将同一短期 Grant 固定为 PTY+Files，Access Gateway 提供 `/workspace` 相对路径的列表、1 MiB 版本绑定分页读取、16 MiB 写入和普通文件删除；路径穿越、symlink 穿越、错误 token、跨 tenant、超限和删除后读取均真实拒绝。真实文件在 Gateway 重启后按 offset/version 续读成功。Admin API/Web 仅显示 7 次尝试、2 次失败及最近 `NOT_FOUND` 等诊断元数据，不返回路径、文件名或内容；最终测试资源为 0。见 [Files 与 Admin 诊断证据](evidence/base-m2-sandbox-files-20260906.md)。本切片未做 Admin 浏览器视觉矩阵，不表示 BASE-M2 完成。
- BASE-M2 私有 Preview 垂直切片已接通：product-000060 持久化 Grant 下的显式活动端口，生成 SDK/CLI 只返回相对 Gateway 路径；Gateway 仅代理固定 OpenSandbox runtime/port，剥离 Grant、Cookie、候选 key 和用户伪造 forwarding 值。真实端口 3000 经 POST 方法/路径/query 转发，Gateway 重启后恢复；错误 token、跨 tenant、未注册/内部端口、端口/Grant 吊销和过期均拒绝，端口吊销关闭活跃流。Admin API/Web 只显示活动端口数字且普通用户为 403，最终测试资源为 0。见 [私有 Preview 与 Admin 端口证据](evidence/base-m2-sandbox-preview-20260906.md)。本轮未把 Foundation 尚未绑定的 Network Policy 显示成已执行。
- BASE-M2 短期 SSH 垂直切片已接通：同一 generation-bound Grant 返回固定 tenant/project/Grant username，短期 token 作为 password；独立 Access Gateway 以部署 host key 提供真实 SSH 协议，只允许一个固定 Sandbox `/workspace` session，拒绝 direct-tcpip、environment 和任意客户主机 shell。真实 SSH 执行、Gateway 重启重连、错误 password、跨 tenant、旧 generation、过期/吊销和活跃 transport 吊销关闭均通过；Admin 只显示 PTY/SSH 会话计数且原始响应不含 username、token、命令或输出。见 [短期 SSH 与 Admin 诊断证据](evidence/base-m2-sandbox-ssh-20260906.md)。
- BASE-M2 网络策略执行垂直切片已接通：product-000061 将规范化 direct allow targets 绑定到 no-Agent RuntimeProfile，Controller 下发后通过认证 sidecar 核对 exact policy 与 `dns+nft` 才结算。真实受限 Sandbox 只连通指定 sink，其他 Sandbox、Docker host/Control Plane 与 metadata IP 均阻断；`previewEnabled=false` 时新端口注册返回 403。Admin API/Web 管理 direct targets 并显示 Profile binding 与 Sandbox enforcement，不向 User API 暴露基础设施 authority；普通用户 Admin API 仍为 403。见 [网络策略执行与 Admin 证据](evidence/base-m2-sandbox-network-policy-20260906.md)。结合 Exec、PTY、Files、Preview、SSH 与 Gateway 证据，BASE-M2 当前固定范围已覆盖。
- BASE-M3 enrollment 垂直切片已接通：product-000062 持久化短期 RemoteWorker 注册意图、一次性 Secret 摘要、resourceVersion、幂等和 Audit；Admin API/Web 只管理元数据和撤销，独立 `remote-worker-bootstrap.act` CLI 路由一次领取 Secret 后拒绝重放。PostgreSQL 17.6 实测普通用户 Admin 403、Admin 领取 403、bootstrap Admin 读取拒绝、`no-store`、数据库无 Secret 原文及 create/claim/revoke Audit；见 [enrollment authority 证据](evidence/base-m3-remote-worker-enrollment-20260906.md)。尚无 CSR、mTLS 节点身份或 outbound 客户节点连接，因此 BASE-M3 保持进行中。
- BASE-M3 首条节点身份签发已接通：product-000063 以独立 enrollment Secret 鉴权，校验节点 CSR 后签发 15 分钟、server-owned SPIFFE SAN 的 mTLS client certificate；PostgreSQL 17.6 实测 000062→000063、全新安装/no-op、证书与本地私钥匹配、原响应精确重放、错误 Secret 401、Audit 和 Admin 元数据脱敏。生产 Control Plane/Helm 已支持配对 CA Secret 与 trust domain，CLI 在请求前预留新 0600 identity file；见 [CSR 与短期节点身份证据](evidence/base-m3-remote-worker-certificate-20260906.md)。尚无轮换/活动证书吊销或 outbound 客户节点连接。
- BASE-M3 活动证书轮换/吊销已接通：product-000064 由 TLS 校验后的当前节点证书授权轮换，PostgreSQL 保存当前/上一指纹并只允许上一证书对同一幂等键和请求摘要精确重放；Admin 沿用带 resourceVersion、资源名确认、幂等和 Audit 的撤销动作。PostgreSQL 17.6 实测 000063→000064、全新安装/no-op、真实 mTLS 轮换、旧证书非重放 401、普通用户 Admin 403、活动证书撤销后节点轮换 401，以及 Admin 只返回状态/时间而不返回证书或 Secret bytes；见 [轮换与吊销证据](evidence/base-m3-remote-worker-certificate-rotation-20260906.md)。本切片没有运行 outbound 客户节点或命令通道。
- BASE-M3 outbound heartbeat/节点健康切片已接通：product-000065 以当前短期 mTLS 证书接受五秒 heartbeat，在 PostgreSQL 持久化节点版本、能力、容量和 30 秒租约，并由数据库时间向 Admin 计算 online/degraded/offline；独立 Linux `cloud-agents-remote-worker` 进程真实出站上报且具备 1–30 秒退避重连，Admin Web 双语展示安全元数据。PostgreSQL 17.6 实测 000064→000065、全新安装/no-op、真实进程 heartbeat、重连客户端、轮换清除旧 incarnation 状态、401/409 负向路径、健康过期/恢复和 Admin 脱敏；见 [heartbeat 与节点健康证据](evidence/base-m3-remote-worker-heartbeat-20260906.md)。真实 harness 使用 `--once`，未模拟定时网络中断恢复，也尚无 command/Drain/Resume。
- BASE-M3 节点 Drain/Resume 命令切片已接通：product-000066 原子推进 server-owned desired generation，经 mTLS heartbeat 下发 30 秒 deadline command；RemoteWorker 以 `0600` 原子状态文件跨重启保存 observed generation/state、去重和 receipt，Control Plane 以 receipt 结算 Operation/Audit。PostgreSQL 17.6 与真实短生命周期进程实测 Drain、重启、Resume、精确请求/回执重放、旧 generation/resourceVersion、错误回执、过期失败和普通用户 Admin 403；Admin Web 使用生成 SDK 完成服务端 preview、精确 enrollment 确认和状态反馈。见 [命令与 Drain/Resume 证据](evidence/base-m3-remote-worker-command-20260906.md)。本切片只改变节点调度状态，尚未在客户节点执行 Workspace/Sandbox workload。
- BASE-M3 RemoteWorker Target 投影切片已接通：product-000067 为每个 enrollment 生成 server-owned `remote-worker` DeploymentTarget，数据库时钟与 mTLS heartbeat/certificate authority 驱动 unprobed/ready/offline/reconnect/revoked 状态；普通用户 Admin Target 读取 403，通用 Target Probe/Cleanup/Drain/Resume 均 409，避免绕过 RemoteWorker 生命周期。PostgreSQL 17.6 实测 000066→000067、fresh/no-op、真实 outbound 进程与投影状态；Admin Web 用生成 SDK 展示 placement target 并禁用错误入口。见 [Target 投影证据](evidence/base-m3-remote-worker-target-20260906.md)。本切片尚未向客户节点下发 Workspace/Sandbox workload。
- BASE-M3 outbound Sandbox create 切片已接通：product-000068 将在线、active 且具备 Docker capability 的 RemoteWorker 纳入 no-Agent RuntimeProfile publish 与 User Sandbox admission；heartbeat 只下发无 endpoint/credential 的持久化 `sandbox.create` attempt，节点使用本地 Docker/OpenSandbox 配置复用 Foundation executor，并以当前 mTLS 指纹结算 Audit。PostgreSQL 17.6 实测 000067→000068、fresh/no-op；OrbStack 上真实独立 RemoteWorker 创建 Running OpenSandbox runtime、保留 Workspace volume、重复两次回执，并精准清理至零测试残留。见 [客户节点 Sandbox create 证据](evidence/base-m3-remote-worker-sandbox-create-20260906.md)。
- BASE-M3 outbound Sandbox Stop 切片已接通：product-000069 复用同一 durable Operation/outbox 和 Foundation executor，经当前 mTLS 节点下发绑定旧 runtime、generation、spec digest 与保留卷的 `sandbox.stop`；结算同时校验 action/command/target/incarnation，成功后释放 writer 并允许精确回执重放。OrbStack Docker 29.4.0、PostgreSQL 17.6 与真实独立 RemoteWorker 实测物理删除、Admin `stopped`、保留卷、四条节点 Audit 及零测试残留；已有 Admin Web Stop 确认/Operation 流程直接适用并通过生成 SDK、33 项测试和构建。见 [客户节点 Sandbox Stop 证据](evidence/base-m3-remote-worker-sandbox-stop-20260906.md)。
- BASE-M3 outbound Sandbox Rebuild 切片已接通：product-000070 让既有 Admin generation/resourceVersion 围栏接受在线 active RemoteWorker，并下发仅绑定保留卷、不携带旧 runtime 的 `sandbox.rebuild`；客户节点复用 Foundation executor 创建新 generation runtime，回执按 action/attempt/Target/incarnation 结算和重放。OrbStack Docker 29.4.0、PostgreSQL 17.6 与真实独立 mTLS RemoteWorker 实测 Stop 前写入、同卷 Rebuild 后 SHA-256 不变、再次 Stop 及零测试残留；已有 Admin Web Rebuild 确认/Operation 流程通过生成 SDK、33 项测试和构建。见 [客户节点 Sandbox Rebuild 证据](evidence/base-m3-remote-worker-sandbox-rebuild-20260907.md)。
- BASE-M3 reverse Sandbox Exec 切片已接通：product-000071 在既有 User Exec contract 后按 PostgreSQL project/generation authority 排队，经当前 mTLS incarnation 的 outbound heartbeat 下发，客户节点复用 node-local OpenSandbox 在 `/workspace` 执行并结算 bounded stdout/stderr/exit/duration；Admin Exec 403，命令/输出表对 runtime role 不可读。OrbStack Docker 29.4.0、PostgreSQL 17.6 与真实独立 RemoteWorker 实测 exit 7、保留 Workspace SHA-256、请求/回执精确重放与冲突拒绝、证书/incarnation 绑定及最终零容器/卷残留。见 [客户节点 Sandbox Exec 证据](evidence/base-m3-remote-worker-sandbox-exec-20260907.md)。
- BASE-M3 reverse Sandbox Files 切片已接通：product-000072 保持既有 User Files API，经 generation-bound Grant、PostgreSQL authority 和当前 mTLS heartbeat 在客户节点执行 list/read/write/delete；1.9 MiB 写入跨越原 heartbeat 2 MiB 响应上限，Gateway 重启后按 version/offset 分页读回。错误 token/跨租户为 403，symlink 与 authority-mismatched receipt 为 409，删除后读取为 404；Admin 仅见计数/稳定错误且 runtime role 不可读路径或内容，最终零容器/卷残留。见 [客户节点 Sandbox Files 证据](evidence/base-m3-remote-worker-sandbox-files-20260907.md)。
- BASE-M3 reverse Sandbox PTY 切片已接通：product-000073 保持既有 User PTY API 与 Access Gateway 路径，经 generation-bound Grant、PostgreSQL authority 和当前 mTLS heartbeat 在客户节点交换 bounded WebSocket frames。真实 `/workspace` PTY 在 Gateway 重启后按 cursor 回放并继续输入；错误 token/跨租户/删除后访问为 403，authority mismatch 与 changed receipt 为 409。7 条命令均绑定 incarnation/certificate 并结算，runtime role 不可读帧，Admin 只见 1 个 session 计数，最终零容器/卷残留。见 [客户节点 Sandbox PTY 证据](evidence/base-m3-remote-worker-sandbox-pty-20260907.md)。
- BASE-M3 reverse Sandbox Preview 切片已接通：product-000074 保持既有 User Preview API 与相对 Gateway 路径，经 generation-bound Grant、Network Policy、PostgreSQL authority 和当前 mTLS heartbeat 在客户节点代理 bounded HTTP。真实 POST method/path/query/body 通过，credential/cookie/forwarding/`Set-Cookie` 均剥离；Gateway 重启、撤销、403/404/409 负向路径及 receipt 重放通过。2 条命令均绑定 incarnation/certificate 并结算，runtime 与 Admin 不可读请求/响应内容，最终零容器/卷残留。见 [客户节点 Sandbox Preview 证据](evidence/base-m3-remote-worker-sandbox-preview-20260907.md)。
- BASE-M3 RemoteWorker SSH 切片已接通：product-000075 复用既有短期 Grant、SSH Gateway 与 RemoteWorker PTY command table，在首次 node-local OpenSandbox WebSocket attach 后以 bounded stdin frame 驱动 PTY shell 和非 PTY exec；浏览器和 Control Plane 均不直连客户节点。OrbStack Docker 29.4.0、PostgreSQL 17.6 与真实 SSH 协议实测 `/workspace`、stdout/stderr、exit 7、Gateway 重启、PTY/pipe 双模式；缺少 `ssh` capability、错误 password、跨 tenant、direct-tcpip、environment 均拒绝，命令绑定当前 incarnation/certificate 且 runtime/Admin 不可读内容，最终零容器/卷残留。见 [客户节点 Sandbox SSH 证据](evidence/base-m3-remote-worker-sandbox-ssh-20260907/evidence.json)。
- BASE-M3 长时 claim 与断线重连切片已接通：product-000076 让执行中的 lifecycle command 通过现有五秒 mTLS heartbeat 每 20 秒续租既有 PostgreSQL claim；本地 `0600` 原子状态只记录当前已开始 command，进程重启不盲目重放。OrbStack Docker 29.4.0 与 PostgreSQL 17.6 故障注入实测 OpenSandbox create 阻塞超过原 claim、错误 command ID 返回 409、第二次续租连接被切断、原 command 不结算，claim 自然过期后仅以 attempt 2 对账执行；Workspace 生命周期及 Exec/Files/PTY/Preview/SSH 全回归通过并精确清理至零测试残留。见 [长时 claim 与断线重连证据](evidence/base-m3-remote-worker-reconnect-20260907/evidence.json)。
- BASE-M4 首个 Kubernetes Foundation 切片已接通：product-000077 让既有 Target/Profile/User Sandbox authority 接受 direct Kubernetes，Control Plane 以专用 ServiceAccount 创建并核验 owner-bound 保留 PVC，经固定 OpenSandbox server/controller 创建真实 BatchSandbox；既有生成 SDK 完成 Target 注册/Probe、Profile 发布、Sandbox create/Exec/Stop/Rebuild/final Stop，普通用户 Admin 403、Admin Exec 403、旧 generation 409，最终测试 Pod/BatchSandbox/命名空间/RBAC/容器均为零且 Workspace PVC 仅在测试命名空间生命周期内保留。OrbStack Kubernetes 1.35.6 与 PostgreSQL 17.6 的固定镜像、摘要和边界见 [Kubernetes Foundation 证据](evidence/base-m4-kubernetes-foundation-20260907/evidence.json)。
- BASE-M4 RemoteWorker 能力准入切片已接通：product-000078 扩展 heartbeat contract/生成 SDK，以数据库 authority 在 Profile 发布、公开列表、Sandbox 创建/重建和 Controller claim 统一校验 Docker runtime、amd64/arm64、`workspace-volume`、`network-dns-nft` 及 Profile CPU/内存和固定 20 GiB Workspace 磁盘下限；能力漂移时 User API 隐藏 Profile 且不下发命令。Admin Web 在既有节点详情显示同一 Foundation 基线和明确缺失原因。PostgreSQL 17.6 实测 fresh、000077→000078、no-op；真实 OrbStack RemoteWorker 对七类不兼容声明返回 409，恢复后完成 Sandbox/Exec/Files/Preview/PTY/SSH/Stop/Rebuild/Cleanup，最终测试容器和卷为零；见 [RemoteWorker 能力准入证据](evidence/base-m4-remote-worker-capability-admission-20260907/evidence.json)。当前架构值仅代表 worker 进程，容量按单 Sandbox 判定，尚不代表镜像 manifest 架构或聚合资源预留。
- BASE-M4 首条 Region/Pool/Node 聚合容量切片已接通：product-000079 用 PostgreSQL authority 将真实 RemoteWorker 投影到固定 `region-local` / `pool-remote-worker` / server-owned Node，并按 desired-running Sandbox 汇总 CPU/内存、按长期 Workspace 固定 20 GiB 汇总存储；User Profile 列表、Sandbox 创建/重建和命令 claim 共用聚合准入，创建/重建在既有 SERIALIZABLE mutation 内对 Target 加行锁。Admin Web 通过原 RemoteWorker 页面展示总量、预留、可用和 available/exhausted/overcommitted 状态，不新增空 Region 页面。PostgreSQL 17.6 实测 fresh、000078→000079、no-op；真实 OrbStack RemoteWorker 将容量降至现有 Sandbox 恰好占满后，User API 隐藏 Profile、第二个 Sandbox 返回 409 且 Workspace 事务零残留，恢复容量后完整生命周期继续并最终零容器/卷；见 [聚合容量证据](evidence/base-m4-remote-worker-capacity-reservation-20260907-v2/evidence.json)。当前只覆盖一个真实 RemoteWorker Pool；不代表多 Node 确定性选择、镜像 manifest 架构或强隔离 runtime。
- BASE-M4 RemoteWorker 多 Node 确定性选择切片已接通：product-000080 让 RuntimeProfile 在 direct Target 外可选择固定 `region-local` / `pool-remote-worker` / Docker / 架构，由 PostgreSQL authority 硬过滤 owner、健康、能力、容量和 Workspace 卷可达性，并按 CPU/内存/存储可用量及 Target UID 稳定排序；Sandbox 首次选择后继续绑定同一 Target，Stop/Rebuild 不漂移。Admin Web 复用 Profile 表单、列表和详情展示选择条件，不新增 Region/Pool 空 CRUD；User API 仍只暴露脱敏摘要且请求保持 profile ID/version。PostgreSQL 17.6 实测 fresh、000079→000080、no-op；两台真实 arm64 mTLS RemoteWorker（4k/8 GiB/40 GiB 与 8k/16 GiB/80 GiB）验证选择后者、前者不接收命令、错误 region/pool/arch 均 409、完整 Sandbox/Exec/Files/Preview/PTY/SSH/Stop/Rebuild 及最终零容器/卷，见 [多 Node 选择证据](evidence/base-m4-remote-worker-node-selection-20260908/evidence.json)。当前架构值仍只代表 worker 进程，不代表镜像 manifest 架构或强隔离 runtime。
- BASE-M4 强隔离切片已接通：product-000081 让 RuntimeProfile 必须选择 `trusted-single-tenant`、`dedicated-node` 或 `shared-untrusted`，并由数据库 authority 将其分别约束为 `runc`、具备 `dedicated-node` 能力的 `runc`、或具备 `isolation-gvisor` 与 `network-internal-deny` 能力的 `gvisor`；共享不可信租户只允许 deny-only/空 egress/无 Preview，不静默降级。RemoteWorker 复用现有 Foundation controller 以 `runsc` 创建 OpenSandbox，结算前物理核验 runtime、唯一 internal network 与 ICC=false；不匹配则失败并清理。Admin Web 复用 Profile 与 Sandbox 视图展示信任/隔离等级及限制，User API 保持脱敏。PostgreSQL 17.6 实测 fresh、000080→000081、no-op；固定 ARM64 `runsc release-20260622.0` 与隔离 DinD 实测 kernel 为 gVisor、外网阻断、停止及零测试容器/卷，见 [gVisor 强隔离证据](evidence/base-m4-strong-isolation-gvisor-20260908/evidence.json)。该证据不声称 MicroVM、多 Region 或多架构 OCI manifest，均非 BASE-M4 退出必需项。
- BASE-M5 首个 Docker Workspace Snapshot 创建切片已接通：product-000082 只接受 stopped/observed stopped/writer-released 的来源 Sandbox，并复用单 writer authority 在复制期间阻止 rebuild；Controller 通过从不启动的 helper 仅挂载来源 Workspace 与目标 Snapshot volume，使用规范化路径、类型、模式、链接和文件字节摘要核验复制，Secret/credential volume 不进入归档。Admin API/生成 SDK/Admin Web 只展示来源 Workspace、容量、状态和 Operation，普通用户返回 403，物理 volume、内容摘要、端点和凭据不返回浏览器。PostgreSQL 17.6 与 OrbStack Docker 29.4.0 实测 pending 期间 rebuild=409、终态 fence 释放、Operation/Audit 成功且最终相关容器和卷为零；见 [Docker Workspace Snapshot 证据](evidence/base-m5-workspace-snapshot-docker-20260908-r7/evidence.json)。
- BASE-M5 的 Docker 离线恢复切片已接通：Admin 只提交 Snapshot resourceVersion、新 Workspace/Sandbox 和已发布 RuntimeProfile；服务端绑定 Snapshot 内部物理卷/内容摘要与同一来源 Docker Target，沿用 Sandbox Operation、单 writer fence、幂等键和 Audit。Controller 只创建新的确定性 Workspace volume，通过从不启动的 helper 校验归档摘要后复制，已有非同内容目标拒绝覆盖；Admin/User API 均不返回 Snapshot 内部字段，普通用户调用 Admin restore 为 403。PostgreSQL 17.6 与 OrbStack Docker 29.4.0 实测 stale resourceVersion=409、幂等重放、真实 Sandbox 读取原 proof 文件、stop 释放 writer，并在验证后清零相关容器和卷；见 [Docker Workspace Snapshot Restore 证据](evidence/base-m5-workspace-restore-docker-20260908-r4/evidence.json)。
- BASE-M5 Snapshot retention/清理切片已接通：product-000084 在数据库时钟上固定 1 秒至 365 天的 retention，手动删除与到期回收共用 snapshot resourceVersion fence、幂等 Operation、可续租 claim、Audit 与 tombstone；失败终态保留逻辑 Snapshot 并以新 resourceVersion/generation 重试，不向 Admin 返回物理卷、内容摘要、endpoint 或凭据。PostgreSQL 17.6 与 OrbStack Docker 29.4.0 实测普通用户 403、旧版本 409、失败/续租 fencing、手动及到期删除、terminal receipt 和零测试容器/Workspace/Snapshot 卷残留；真实 Chromium 145 通过 Vite/Control Plane Admin API 完成英/中、桌面/移动、深色主题和危险确认，浏览器仅访问 Admin origin、无 console/page error 且不持久化 Token。见 [Snapshot retention/清理证据](evidence/base-m5-workspace-snapshot-retention-docker-20260908-r19/evidence.json)。
- BASE-M5 当前迁移头 Compose 安装切片已接通：发布脚本从 product migration 目录派生 `000084`，迁移/部署制品名及打包后的 migrate image 输入不再手写旧版本；合法累计 JSON catalog 的单成员上限由 1 MiB 调整为 2 MiB，总归档仍固定 64 MiB。Compose 以无网络 one-shot 将部署方 32–64 字节 access-grant key 复制为 uid `65532`、`0400` 的独立只读 secret volume，Control Plane 不直接挂载宿主 key。OrbStack Docker 29.4.0、Compose v5.1.2 与 PostgreSQL 17.6 实测 fresh migration、no-op 重跑、权限/策略/配额、Target/Profile/Environment/Worker 运维、重启、备份/恢复及最终零测试容器和卷；见 [当前迁移头 Compose 证据](evidence/base-m5-compose-current-migrations-20260908-r2/evidence.json)。该证据包当时未注入真实 Provider，Kubernetes 为 API fixture，且尚不含 Access Gateway/Admin Web，不能声明独立交付完成。
- BASE-M5 Compose Access Gateway 切片已接通：固定 linux-amd64/arm64 Gateway 二进制与 distroless nonroot Dockerfile 进入发布包；Compose Gateway 以 uid `65532`、只读根文件系统、全部 capability drop、`no-new-privileges`、独立只读 SSH host-key volume 且无 Docker socket 运行。OrbStack Docker 29.4.0、Compose v5.1.2、PostgreSQL 17.6 与固定 OpenSandbox 实测发布 Profile、创建 Sandbox/Grant、HTTPS Files/PTY/private Preview、严格 host-key SSH、Gateway 重启、Stop/单写释放/网络停止、备份恢复后双服务 ready，并按 OpenSandbox 原生 inventory 回到零新增 runtime、egress 和卷；见 [Compose Access Gateway 证据](evidence/base-m5-compose-access-gateway-20260908-r4/evidence.json)。未注入真实 Provider，Kubernetes 仍为 API fixture，本切片不是完整独立交付。
- BASE-M5 Compose Admin Web 切片已接通：发布脚本先重建生成 TypeScript SDK 与独立 Vite Admin Web，再把静态资源、Node 标准库同源服务和 nonroot Dockerfile 纳入部署包；Compose Admin Web 仅挂 Control Plane CA，以 uid `1000`、只读根文件系统、全部 capability drop、`no-new-privileges` 且无 Docker/Target/Provider 凭据运行，只代理 `/v1/admin`。OrbStack Docker 29.4.0、Compose v5.1.2、PostgreSQL 17.6 与真实 Brave 152 验证 17 个同源 Admin 请求、普通用户 403、Token 不落浏览器存储、英文与深/浅主题、1440/390 无横向溢出；同轮 Gateway 数据面、重启、备份恢复与最终零残留通过，见 [Compose Admin Web 证据](evidence/base-m5-compose-admin-web-20260908-r3/evidence.json)。该本地 loopback 证据不替代 Helm/OIDC ingress 或完整 Admin 验收。
- BASE-M5 Helm Admin Web/Access Gateway 切片已接通：当前发布包纳入两套 Deployment/Service/NetworkPolicy、必需 access-grant key 和可复现集群 smoke；服务 Pod 与 migration/bootstrap Hook 均关闭 ServiceAccount token，Admin 仅挂 Control Plane CA，私钥由 non-root init container 复制到内存卷并固定 `0400`。本地候选 `0.3.0-dev.89` 的 20 个 checksum 与打包文件一致；真实 OrbStack Kubernetes `v1.35.6+orb1`、Helm `v4.2.4`、PostgreSQL 17.6 验证 84 条迁移、四个 Deployment ready、普通用户 403、真实 Brave 14 个同源 Admin 请求、Gateway TLS/SSH identity、重启恢复和零测试 namespace/PV/image tag，见 [Helm Admin Web/Gateway 证据](evidence/base-m5-helm-admin-web-20260908-r5/evidence.json)。本轮未做 OIDC ingress、Kubernetes Target/Sandbox 数据面、备份恢复或升级回滚。
- BASE-M5 客户节点 bootstrap 切片已接通：发布 manifest 增加版本化 linux-amd64/arm64 `cloud-agents-remote-worker`，部署归档增加 POSIX bootstrap 与运行手册；脚本从独立 bootstrap Token 一次性领取 Secret，在节点本地生成 key/CSR、签发短期 mTLS identity，成功后删除 Secret，并输出可由既有 supervisor 前台托管的无监听进程入口。候选 `0.3.0-dev.94` 的 22 个 checksum 与打包脚本/文档一致；固定 digest 的全新 Debian 容器对真实 Helm Control Plane 完成 enrollment 201、Secret/证书 200、持续 outbound heartbeat 200，Admin 投影 online 且浏览器同源请求增至 15，节点无发布端口，安装目录/identity/CA/入口权限为 `0700/0600/0400/0500`，最终 namespace/PV/容器/测试镜像均为零；见 [客户节点 bootstrap 证据](evidence/base-m5-remote-worker-bootstrap-20260908-r5/evidence.json)。该轮容器节点仅声明 `exec`，未接本地 Docker/OpenSandbox、未执行 Sandbox，且当时尚未自动轮换证书。
- BASE-M5 同迁移头 Helm N/N-1 切片已接通：复用打包 smoke，以声明兼容范围内的 `0.1.0-alpha.94`/`0.1.0-alpha.95` 两套 22-artifact manifest 和精确镜像，在同一 PostgreSQL、Secret 与 PVC 上完成 N-1 安装、N 升级、N-1 回滚和 N 再升级；每一步核对四个服务及 migration init image，并读回同一 Project。最终 N 再通过 RemoteWorker、Admin Web 浏览器 15 请求、普通用户 403、Gateway、重启和零 namespace/PV/容器/测试镜像；见 [Helm N/N-1 证据](evidence/base-m5-helm-upgrade-rollback-20260908-r4/evidence.json)。两版 schema 均为 000084，Chart 使用 `Recreate`，因此该记录不证明跨 schema-head 或零停机 rolling availability。
- BASE-M5 客户节点自动身份轮换切片已接通：RemoteWorker 复用既有 mTLS 轮换 API，在到期前五分钟续签，并以本地 `0600` pending 请求、原子 identity/resource-version 文件和确定性幂等键支持不确定响应后的精确重放。候选 `0.1.0-alpha.100` 的 22 个 checksum 与打包脚本/运行手册一致；固定 digest Debian 客户节点对真实 Helm Control Plane 从资源版本 3 轮换到 4，新证书 heartbeat/重启成功，旧证书返回 401，pending 清除，Admin Web 浏览器、Gateway、普通用户 403、重启和最终零残留均通过；见 [客户节点自动身份轮换证据](evidence/base-m5-remote-worker-identity-rotation-20260908-r1/evidence.json)。本轮用强制单次开关触发同一续签路径，没有等待默认墙钟阈值，也不覆盖 CA trust root 或部署服务证书轮换。
- BASE-M5 RemoteWorker CA 信任根轮换切片已接通：沿用部署方 Secret authority，以不同名不可变 Secret 和 Helm 引用切换执行旧根→新旧重叠→仅新根；Control Plane 在重叠期继续接受旧 leaf 并用新根签发资源版本 4，移除旧根后新 leaf heartbeat/进程重启成功、旧 leaf 返回 401，最终 Secret 字节与 Deployment 引用精确核对。候选 `0.1.0-alpha.105` 的 22 个 checksum 与打包 runbook/smoke 一致；真实 OrbStack Kubernetes、PostgreSQL 17.6、固定 digest Debian 节点、Admin Web 浏览器、Gateway、普通用户 403 及 namespace/PV/容器/测试镜像零残留均通过；见 [RemoteWorker CA 轮换证据](evidence/base-m5-remote-worker-ca-rotation-20260908-r1/evidence.json)。Chart 使用 `Recreate`，本轮不声明零停机，也尚未覆盖 Control Plane/Worker/Gateway/Admin 服务证书根。
- BASE-M5 部署服务证书/CA 轮换切片已接通：沿用 Chart 现有外部 Secret 引用，以不同名不可变 Secret 完成旧 leaves＋新旧根、全新 leaves＋新旧根、全新 leaves＋仅新根三阶段；逐阶段核对 Control Plane、Worker、Access Gateway 与 Admin Web Deployment 引用，并真实验证 Admin 代理、Control Plane→Worker 及直接 Worker mTLS、Gateway TLS/SSH、固定 digest Debian 节点 heartbeat。移除旧根后旧 Control Plane/Gateway server root、旧 Worker client 与节点旧 server root 均被拒绝，最终 CA 字节精确匹配。候选 `0.1.0-alpha.109` 的 22 个 checksum 与打包 runbook/smoke 一致；任务自有 kind Kubernetes v1.37.0、PostgreSQL 17.6、真实 Brave、普通用户 403、重启及 namespace/PV/容器/测试镜像零残留通过，临时集群已删除并恢复原 `orbstack` context；见 [部署服务 CA 轮换证据](evidence/base-m5-service-certificate-rotation-20260908-r1/evidence.json)。Chart 使用 `Recreate`，本轮不声明零停机；kubectl v1.33.9 对 v1.37.0 有超支持范围版本偏差警告，且本轮不覆盖外部证书签发器或其他平台密钥轮换。
- BASE-M5 Sandbox CPU/内存分配事实切片已接通：product-000085 按 Sandbox generation/runtime UID 以数据库时钟持久化 checkpoint，Controller 重启后补齐离线时段，Stop 在同一事务中冻结累计分配毫秒、mCPU·毫秒和 byte·毫秒；Admin API/生成 SDK 以十进制字符串保留大整数，Admin Web 只展示分配事实和 checkpoint/finalized 状态，User API 未增加用量或基础设施字段。PostgreSQL 17.6 与 OrbStack Docker 29.4.0 实测重启补记超过 120 秒、CPU/内存权重精确相乘、Stop 后不再增长、普通用户 Admin 403、Snapshot 回归及零测试容器/卷；见 [Sandbox 用量证据](evidence/base-m5-sandbox-usage-docker-20260908-r3/evidence.json)。本轮未重跑浏览器视觉验收，也不包含卷占用、网络用量、计费用量或人工修正。
- BASE-M5 Docker Workspace 卷占用事实切片已接通：product-000086 由 Controller 对 ready Docker Target 的 retained Workspace volume 取得 60 秒 generation claim，先核验确定性名称与完整 owner labels，再读取 Docker `GET /system/df?type=volume` 的后端 `UsageData.Size`，不打开 Workspace 文件；一分钟 checkpoint、失败保留最近成功值和稳定错误均落 PostgreSQL。Admin API/生成 SDK/Admin Web 只展示来源、状态、测量代次、占用字节和时间，User API 未增加字段。PostgreSQL 17.6 实测 fresh、000085→000086、no-op、claim/settle/RLS/失败保留；OrbStack Docker 29.4.0 实测真实 31 bytes、Controller 离线两分钟后代次 1→2 补账、普通用户 403、Snapshot 回归及零测试容器/卷，见 [Workspace 卷占用证据](evidence/base-m5-workspace-volume-usage-docker-20260908-r1/evidence.json)。该值不是容量配额或可计费值；Kubernetes/RemoteWorker 卷测量和浏览器视觉验收未覆盖。
- BASE-M5 Docker Sandbox 网络用量事实切片已接通：product-000087 复用 Sandbox usage checkpoint，对 ready direct Docker Target 的当前 Running runtime 取得 60 秒 generation claim，并从 exact-owned container 的 Docker stats 汇总累计 RX/TX 字节；不采集 packet、地址、目标或 payload。计数单调校验、一分钟 checkpoint、失败保留最近成功样本与稳定错误均落 PostgreSQL；Admin API/生成 SDK/Admin Web 只展示来源、runtime/测量代次、状态、聚合字节和时间，User API 未增加字段。PostgreSQL 17.6 实测 fresh、000086→000087、no-op、claim/settle/RLS/失败保留；OrbStack Docker 29.4.0 实测 restart 后 RX 8872→10100、TX 6565→7965、代次 1→2、普通用户 Admin 403、Stop 后保留最近成功样本、Snapshot 回归及零测试容器/卷，见 [Sandbox 网络用量证据](evidence/base-m5-sandbox-network-usage-docker-20260908-r1/evidence.json)。该值不是配额或可计费终值；Kubernetes/RemoteWorker 网络测量和浏览器视觉验收未覆盖。
- BASE-M5 可审计用量修正切片已接通：product-000088 为已存在的 Sandbox CPU/内存、Workspace 卷及网络原始事实追加不可变有符号 delta，不修改原事实，也不引入价格、Wallet、Invoice 或 Billing；Admin 写入必须携带 generation/resourceVersion、Sandbox 确认、reason code 与幂等键，每个 Sandbox 完整返回最多 100 条后 fail closed。生成 SDK、Control Plane 和 Admin Web 使用同一 contract，普通用户为 403，重复请求返回同一修正，变更重放及旧版本为 409；发布迁移包只携带 product migrator 实际消费的当前 manifest/schema-bundle 与 88 条 SQL，保留源码历史 catalog 完整性校验，并在固定 64 MiB 限制下缩至 2,097,664 bytes。PostgreSQL 17.6、OrbStack Docker 29.4.0 与真实 Chromium 实测原 RX/TX 不变、修正持久化、16 条同源 Admin 请求、1440/390 无横向溢出、Token 不落存储，以及手动/到期 Snapshot 清理后零测试容器和卷；见 [用量修正证据](evidence/base-m5-sandbox-usage-correction-docker-20260909-r5/evidence.json)。Kubernetes/RemoteWorker 用量事实、计费汇总和完整 Admin 视觉/无障碍验收仍未覆盖。
- BASE-M5 Admin 视觉/无障碍联合验收切片已接通：现有 Daytona `v0.190.0` capture 直接复用同轮 PostgreSQL authority、生成 SDK、Vite 同源代理与真实 Docker Snapshot/用量/清理流程，不再依赖固定三行旧 fixture；测试服务器补齐真实 denied-write handler，并有界处理 `RunOne` 中其他先到期工作项。真实 Chromium 152 对 6 个持久化 Target（3 个 RemoteWorker 及 Docker/Kubernetes/SSH）完成 145 张实时截图、24 组固定 Daytona 结构匹配、8 组 zh-CN/en-US × light/dark × 1440/390 无障碍矩阵；表单标签、键盘/焦点/错误恢复、对比度、reduced motion、locale reload/fallback、同源与 Token 不落存储均通过，13 个普通用户 Admin 请求均为 403，仅出现 8 个脚本主动触发并恢复的 cleanup-preview 503，未提交生命周期写入，最终测试容器/网络/卷为零。见 [Admin 联合验收证据](evidence/base-m5-admin-acceptance-docker-20260909-r7/evidence.json)。该证据关闭当前 Admin 视觉、双语与基本无障碍矩阵，不替代 Kubernetes/SSH Snapshot 数据面或聚合 BASE-ADMIN-V1 基础设施验收。
- BASE-M5 Docker Controller/Control Plane 故障与有界 soak 切片已接通：Controller 在真实物理创建后、结算前退出，新进程等待 claim 过期后以同一 Operation/Runtime adopt，保留 Workspace 字节并补偿另一失败 runtime；故障到完整恢复测试结束上界 5750.240 ms，Operation 行和 Workspace 字节 RPO 均为 0。两个独立 Control Plane 测试进程复用生产 Handler、生成 SDK 和同一 PostgreSQL authority，各执行 64 轮六次成功 Admin 读取与一次普通用户 403，共 896 请求；重启前成功读取 P50/P95 为 2.215/3.283 ms，重启后为 2.011/2.998 ms，进程启动至首个成功响应 RTO 为 25.559 ms，Profile/Sandbox/Snapshot 投影摘要跨重启不变且最终零测试残留。见 [Docker 故障与 soak 证据](evidence/base-m5-fault-soak-docker-20260909-r2/evidence.json)。这些是当前机器的有界实测，不是 SLO，也不覆盖写饱和、Kubernetes/SSH/RemoteWorker、外部负载均衡或跨 Region 故障。
- BASE-M5 RemoteWorker/Access Gateway 故障与有界 soak 切片已接通：真实 outbound 连接在长任务续租后被切断，原不确定 command 未重放，claim 自然过期后只执行 attempt 2；断连到 Control Plane 结算为 60311.396 ms，恢复连接到本地 receipt/服务端结算为 192.579/238.517 ms，Operation 行 RPO 为 0 且无重复 runtime。恢复后 64 次 mTLS heartbeat 与 64 次 Admin 读取全部成功，heartbeat P50/P95 为 9.366/11.064 ms，Admin 读取为 2.450/3.105 ms；Preview/Files/PTY/SSH 四次 Gateway 重启分别在 212.549/296.607/227.069/1577.846 ms 恢复，Grant 行、1.9 MiB 文件和 PTY 输出 RPO 为 0。实测同时暴露并修复非 PTY SSH 在首个 250 ms 空闲窗口后仅返回终端回显的竞态：现于既有 deadline 内等待 exit，交互 PTY 分片保持不变。见 [RemoteWorker/Gateway 故障与 soak 证据](evidence/base-m5-remote-worker-gateway-soak-docker-20260909-r2/evidence.json)。数值是当前机器有界实测，不是 SLO。
- BASE-M5 Kubernetes 故障与有界 soak 切片已接通：Cloud Agents Controller 在真实 Kubernetes 物理创建后、结算前退出，新进程 reap 过期 claim 并 adopt 同一 Runtime、Operation 与 PVC；同时重启 OpenSandbox controller Deployment 并删除运行中的 BatchSandbox Pod，两者均以新 UID 恢复，Workspace 摘要不变。当前机器实测 Controller 退出到结算上界 27214.302 ms、OpenSandbox controller Pod 恢复 1882.746 ms、工作负载 Pod 恢复 22239.822 ms，Operation 行和 Workspace 字节 RPO 均为 0。恢复进程完成 64 轮三次生成 SDK Admin 读取与一次普通用户 403，共 256 请求；成功读取 P50/P95 为 1.764/3.069 ms，进程启动至 reap/adopt 后首个成功 Admin 响应为 289.872 ms，投影摘要稳定且最终 Sandbox/BatchSandbox/Pod/PVC/namespace/RBAC/测试容器零残留。见 [Kubernetes 故障与 soak 证据](evidence/base-m5-kubernetes-fault-soak-20260909-r1/evidence.json)。本轮是本地 OrbStack Kubernetes，不覆盖外部集群、SSH、Kubernetes control-plane/node 整体故障、写饱和或跨 Region。
- BASE-M5 外部 SSH Target 兼容与有界 soak 切片已接通：两个既有 key-only SSH alias 先经本机严格 known_hosts 验证，再由两个独立 Control Plane 测试进程复用 PostgreSQL authority、生产 Admin Handler 与生成 SDK 完成 64 次真实 Probe、64 次 Admin 读取和 32 次普通用户 403。重启前 Probe P50/P95 为 1914.307/2602.243 ms、Admin 读取为 6.238/9.169 ms；重启后为 1969.817/2526.386 ms 与 6.452/8.964 ms，进程启动到首个外部 Probe 成功为 2398.760 ms，SSH 服务事实稳定。用另一主机固定 key 请求真实 endpoint 被拒并持久化 `ssh-host-key-mismatch`，Operation/Audit 可追溯；地址和凭据字节未进入证据包，全程未写远端。见 [外部 SSH Probe 证据](evidence/base-m5-external-ssh-probe-soak-20260909-r2/evidence.json)。两台主机均无 Docker，本轮只关闭 BASE-ADMIN-V1 要求的旧 SSH 注册/Probe 兼容，不证明远端 Worker、Provider、SSH 服务重启或旧 ADMIN-WEB-V1 完整 SSH E2E。
- BASE-M5 独立 User/Admin Web 部署切片已接通：发布包新增独立 User Web 镜像、Compose service 与 Helm Deployment/Service/NetworkPolicy；共享最小 Node server 按 scope 只代理 User 或 Admin API。当前 dirty source 生成未发布 `0.3.0-dev.89` 候选，真实 Compose 验证 User same-origin API 200、Admin 路由 404、Admin 浏览器 17 请求/普通用户 403、Docker Worker Upgrade/Rollback、Gateway Files/PTY/Preview/SSH、备份恢复及零本轮资源；OrbStack Helm 验证 88 条迁移、五个 Deployment、User/Admin 双服务、RemoteWorker outbound、节点和服务 CA 轮换/旧根拒绝、重启及零本轮资源。并发 SQLSTATE `40001` 现返回 `409 LEASE_RESOURCE_VERSION_CONFLICT` 并按新 preview 有界重试；无宿主终端的 OpenSSH 零尺寸 PTY 规范化为 80×24。见 [双 Web 部署证据](evidence/base-m5-user-admin-web-deployment-20260909-r1/evidence.json)。本轮未执行 N/N-1、OIDC、外部 SSH/Kubernetes Worker 或真实 Provider Turn；既有空 namespace `cloud-agents-helm-smoke-15421` 非本轮创建且未获清理授权，保持不动。
- BASE-M5 User Web 基础设施边界切片已接通：移除可编辑 Control Plane endpoint，生成 SDK 固定使用部署页面 origin，保存上下文只含 tenant/project，并在启动时改写旧版 `sessionStorage` 以清除 endpoint。未发布 dirty-source `0.3.0-dev.96` 精确候选在真实 Compose 与 OrbStack Helm 中分别由 Brave 发出 6 个同源非 Admin User API 请求；页面和浏览器存储不含 endpoint、credential reference 或 kubeconfig，Token 不落存储，User origin 的 Admin 路由为 404，普通用户 Token 调用 Admin API 仍为 403。同轮 Docker/Gateway、outbound RemoteWorker、服务 CA 轮换、restart 与本轮 namespace/PV/容器/镜像清零通过；见 [User Web 边界证据](evidence/base-m5-user-web-boundary-20260909-r1/evidence.json)。该证据关闭 BASE-ADMIN-V1 第 3 项的当前页面/请求/存储范围，不替代 OIDC、外部 Worker、真实 Provider Turn 或聚合验收。
- ADMIN-M4 User/Admin OIDC audience/scope 边界切片已接通：生产 Control Plane 启动配置必须提供不同的 User/Admin audience，同一 issuer/JWKS 复用现有 immutable verifier 分别装配两套路由；Admin-audience 管理员请求成功、同 audience 普通用户 scope 返回 403，User audience 调 Admin 与 Admin audience 调 User 均返回 401。未发布 dirty-source `0.3.0-dev.97` 候选在真实 Compose 与 OrbStack Helm 中由 Brave 分别验证 18/16 个 Admin 请求和各 6 个 User 请求；同轮 Docker/Gateway、outbound RemoteWorker、服务 CA 轮换、restart 与本轮资源清零通过，见 [OIDC audience 边界证据](evidence/admin-m4-oidc-audience-boundary-20260909-r1/evidence.json)。前两次 Helm 在候选服务启动前遭 OrbStack API 创建 Secret `unexpected EOF`，隔离 ready/Secret 诊断通过后第三次完整通过；不隐藏外部失败。该切片当时仍保留旧 User Target/Lease 路由，后续由下一条移除；它本身不宣称整个 M4 完成。
- ADMIN-M4 User API 基础设施 authority 删除切片已接通：OpenAPI、生成 Go/TypeScript 客户端与生产/localdev mux 不再暴露普通用户 Deployment Target 或原始 Lease create/upgrade/terminate，Compose 对两条旧路径实测 404；User Environment 只提交 Profile ID/version，新增 terminate 也只接受 expected generation，响应保持脱敏，运维 Lease 详情与 Upgrade/Rollback 留在 Admin API。外部 Kubernetes/SSH 验收脚本同步改为 Admin 注册 Target/Release/Policy/Profile、User 创建/终止 Environment、Admin 核对 Lease 与确认 Cleanup。未发布 dirty-source `0.3.0-dev.99` 候选完整通过真实 Compose 与 OrbStack Helm，包含 Profile 发布、失败恢复/重放/终止、User→Admin 403、双 audience 401、浏览器、Docker/Gateway、outbound RemoteWorker、重启/备份恢复及本轮资源清零；前两次 `0.3.0-dev.98` 在后端断言通过后发生 headless 浏览器跨 Web 切页超时，直接制品检查正常，最终通过且已增强超时诊断，不隐藏失败。见 [User API authority 边界证据](evidence/admin-m4-user-api-boundary-20260909-r1/evidence.json)。外部脚本本轮仅语法检查，没有外部 Worker 或真实 Provider 证据。
- BASE-M5 默认 no-Agent 部署切片已接通：Control Plane 生产配置在未提供 Worker/Workspace/admission 时只挂载 Foundation/Admin 路由，部分 Agent 配置仍 fail closed；Compose 默认只启动 PostgreSQL、Control Plane、Access Gateway、User Web、Admin Web，旧 Worker 仅由打包的显式 override 启用；Helm 默认 `worker.enabled=false` 并不渲染 Worker/PVC/NetworkPolicy/Provider 或 admission authority。未发布 dirty-source `0.3.0-dev.103` 候选 22 项校验和通过；真实 Compose 先以删除全部 Agent 变量的 env 启动五服务并核对 `/readyz`、Web assets 和 Worker 缺席，再显式启用兼容 Worker 完成浏览器、Target/Profile/Environment、Gateway、备份恢复和零残留；归档内 Helm 脚本完成 outbound RemoteWorker、双 Web、服务/节点 CA 轮换后关闭 Worker，删除五个 Worker/admission/Provider Secrets，重启 Control Plane 并保持 Project 可读，最终 namespace/PV/测试容器清零。见 [默认 no-Agent 部署证据](evidence/base-m5-no-agent-deployment-20260909-r1/README.md)。不把失败的全量 migration 测试或本地集群冒充外部节点证据。
- BASE-M5 外部 outbound 客户节点闭环已接通：经用户明确授权，`hostdzire-4c6g` 真实部署打包的 PostgreSQL/Control Plane/Access Gateway/User Web/Admin Web，`tianliyun-2c2g` 真实部署 Docker、固定 digest OpenSandbox 与 outbound RemoteWorker。未发布 dirty-source `0.3.0-dev.105` 完成 Profile 发布、Sandbox/长期卷、Exec、Files、PTY、短期 SSH、TTL Stop、同卷 Rebuild、断线 offline/重连、Drain/Resume、Operation/Audit 与普通用户 Admin 403；31-byte 文件 SHA-256 跨 Stop/Rebuild 和断线重连不变。真实运行暴露并由 `ff715138` 修复 `sandbox.create` 重试错误携带 physical volume 导致 heartbeat drift 的共享组装缺陷。最终两端本轮容器、volume、network、image、监听和运行目录均为零，Docker 包按授权保留安装/启用；见 [外部客户节点证据](evidence/base-m5-external-remote-worker-20260909-r1/README.md)。
- BASE-READY / BASE-ADMIN-V1 当前源码逐项审计已收口，见 [聚合审计](evidence/base-ready-audit-20260909-r1/README.md)：十二项 BASE-READY 与十三项 BASE-ADMIN-V1 均有当前源码和真实证据。该结论不批准 Release、Beta/GA 或正式历史 Gate，也不改写已归档 ADMIN-WEB-V1 与 APP-M1 Codex/Claude 验收状态。
- 无关 `.gitignore`、`CLAUDE.md`、04 文档补充、`go.work.sum`、`AGENTS.md`、`docs/img.png` 保留且不纳入本切片；06 下方既有编码约束补充 dirty hunk 同样不纳入。原 ADMIN-WEB-V1 的未完成结论不被本次 BASE 审计改写。

### 已完成的文档整合状态（历史，不重复执行）

- 当前文档位置：主项目 `/Users/huang/devel/project/huang/business/cloud-agents`，分支 `codex/cloud-agents-platform-p0`；整理来源为独立 worktree 的 `codex/foundation-first-docs-20260905` / `10541d6f`，执行时不再依赖临时 worktree。
- DOC-1 / DOC-2：VERIFIED（文档范围）；联合交付边界已统一，入口已精简，三组旧执行清单已归档，一份重复整理报告已删除。
- DOC-3：VERIFIED（静态文档范围）；检查结果见下文。不是 BASE 或正式 Gate closure。
- 复核后的五项修正已完成：限定 Synara 旧 CP 指代，区分单项任务与阶段完成，标注旧 Agent 部署配置，补齐按变更范围选验证的入口，将安装实测明确为 BASE-M5 退出条件；未扩展授权、降低联合验收或开始产品实现。
- 旧提示词引用新版 §15 的范围冲突已在文档中修正：原 ADMIN-M1～M4 采用 ADMIN-WEB-V1，新 BASE 主线采用 BASE-READY 与 BASE-ADMIN-V1；固定验收和显式任务迁移输入已补齐，未发送到其他任务或更新 Goal。
- DOC-4：VERIFIED（文档集成）；依据用户“再次确认目标一致吗，一致的我们开始合并代码”的新授权，以 `1c4a422b` 恢复被撤销的文档基线，再合并 `10541d6f` 的最新修订，无合并冲突。上次合并 `1a7878a2` 及撤销 `ed95007c` 的历史保留；本次不是运行代码实施、push 或其他任务迁移。
- 集成前主分支基线为 `7bd3f4f3`。11 份重叠文档草稿已按原字节及暂存状态备份到 stash `d9cca97ca32f4732656e087f6ca05053269ee062`，不覆盖回新版文档；`.gitignore`、`go.work.sum`、`docs/img.png` 保持原内容与未提交状态，既有代码与其他证据保留。
- 当时文档清单内的安全清理已完成，没有需要靠批量删除历史证据解除的阻塞；当时下一项为 BASE-M0，现已按上方 BASE 记录推进完毕。当前下一项以本页开头为准，文档任务仍不自动启动代码、真实节点或部署动作。
- 当时的 BASE 迁移不覆盖旧 ADMIN-M1～M4 任务；04 现提供后续 Runtime Goal 提示词，保存文档不自动发送提示词或修改原任务/Goal，是否应用由用户明确决定。

### 产品阶段状态

下列状态仅针对新的联合切片，不表示仓内没有可复用实现。只有该阶段的真实基础设施行为、对应 Admin 闭环和检查均完成，才标记 VERIFIED；技术 PoC 不冒充产品完成。

| 阶段       | 状态        | 尚需证明的完成范围                                                                                          |
| ---------- | ----------- | ----------------------------------------------------------------------------------------------------------- |
| BASE-M0    | VERIFIED    | 固定候选、no-Agent Docker PoC、产品执行接缝、幂等 adopt、失败补偿及对应 Admin 运维投影已有固定证据；不表示底座产品已就绪 |
| BASE-M1    | VERIFIED    | 真实长期卷、跨进程恢复、失败补偿、手动与 TTL Stop/Rebuild、单写 fencing、Operation/Audit 和 Admin 保留/到期反馈已实测 |
| BASE-M2    | VERIFIED    | bounded Exec、PTY/Files/private Preview/short-lived SSH、Grant/Gateway、实际网络隔离及对应 Admin 管理已有真实本地 Docker/PostgreSQL 证据 |
| BASE-M3    | VERIFIED    | outbound enrollment/mTLS/轮换吊销、健康与离线拒绝、Drain/Resume、Sandbox lifecycle/Exec/Files/PTY/Preview/SSH、长时续租、断线重连与新 attempt 对账均有真实 Docker/PostgreSQL 证据 |
| BASE-M4    | VERIFIED    | direct Kubernetes、RemoteWorker 能力/容量/多 Node 调度及 shared-untrusted gVisor 强隔离均有真实执行和对应 Admin 证据 |
| BASE-M5    | VERIFIED    | Docker/Kubernetes/外部 outbound 客户节点、长期数据保留恢复、Snapshot、升级回滚、用量、故障恢复、独立部署和 Admin 完整验收均有当前源码证据；不代表发布批准或正式历史 Gate closure |
| BASE-READY | VERIFIED    | [当前逐项审计](evidence/base-ready-audit-20260909-r1/README.md) 已逐项关联十二项 BASE-READY 与十三项 BASE-ADMIN-V1 的真实证据；APP-M1 和旧 ADMIN-WEB-V1 保持独立 |
| APP-M1     | VERIFIED    | ANYWHERE-RUNTIME-V1 的 R1～R5 已通过四 Provider × Docker/outbound RemoteWorker/Kubernetes 真实闭环、跨节点 Workspace 数据恢复、运行中安全接管、故障矩阵、相关 Admin 与仓外 TS/Go SDK；完整用户对话 UI 进入后续 U1 |

## 1. 实现与证据边界

整理时核对的历史源码基线 `40812b8945c8b53a8d71b6bc7ef7c757527bcaea` 已有生产 HTTP/JWT/JWKS 路径、租户/配额、直接 Docker/Kubernetes/SSH actuator、Worker health observer 和 User/Admin Web。该基线仍以 Agent/Lease 为核心：同步部署、随 Lease 清理 volume、入站 mTLS Worker 不等价于目标的独立 Workspace、生命周期 reconciler、outbound RemoteWorker。已有 [Agent M5 实测](../../../apps/user-web/M5-ORBSTACK-E2E.md) 与 [Network Policy 配套记录](evidence/admin-web-network-policy-20260905.md) 仅证明各自固定范围；本次未重跑运行时验收。

合并只核对相对主分支集成前 `7bd3f4f3` 的变更均为文档，不代表已重新验收该基线及并行改动的运行行为。后续进入实现任务时须重新固定该任务的 HEAD/dirty/backend/工具版本，只复用仍适用的结论；不用历史未验收推断当前代码不存在。

上述文档整合阶段只改变文档，未运行基础设施 PoC、生产服务、真实客户节点或 Provider E2E，未迁移旧卷、部署发布或关闭正式 Gate。后续已授权实现结果以本页“当前实施任务”为准。

## 2. 文档收口问题与处置

| 风险来源                                                      | 可能影响                                                                  | 本轮处置                                                                            |
| ------------------------------------------------------------- | ------------------------------------------------------------------------- | ----------------------------------------------------------------------------------- |
| 0031 / 01–07 / HTML 的“Admin 配套”                            | 后端先交付，把管理 UI 推迟到用户阶段                                      | 0032 明确基础设施＋Admin 是一个交付对象；每项管理能力需两端闭环                     |
| 多个 README、旧 06 的 `PAUSED` / “HTTP absent” / 旧 checklist | 选错下一步、重复实现，或把历史范围当作全局停止指令                        | 入口只导航，旧 06 归档；当前顺序在 04、状态在本页                                   |
| 04 的旧 P0～P6 和 07 的旧 ADMIN-M1～M4 链                     | 与 BASE 竞争；要求先完成用户 Agent 才做基础设施管理                       | 归档旧链；固定历史报告不变，适用迁移/安全条件继续可查                               |
| 多处重复源码、工具版本和发布状态                              | 局部过期时给出互相矛盾的操作建议                                          | CLAUDE 精简为入口与稳定约束；当前事实核对源码，工具版本依照可执行配置               |
| 上轮文档仍写“完成后合并”                                      | 在用户已撤销后再次自动集成                                                | 撤销历史保留；本次集成只依据用户复核后的新明确授权，不外推实现、发布或其他任务权限  |
| 把旧评审文件当作无用文档删除                                  | 生成器的路径存在性或 review SHA-256 校验失败                              | 保留冻结引用；仅删除不承担约束/证据/生成输入的重复报告，精确清单见 04               |
| 旧 M1～M4 提示词动态引用新版 07 §15                           | 原任务被追加新底座范围，或真实 Provider 验收被误删，导致无法完成/错误完成 | 使用独立的 ADMIN-WEB-V1、BASE-ADMIN-V1 标识；只在明确任务迁移时切换，不改旧证据结论 |

### 文档整合时的检查（2026-09-05）

- 集成后的主项目文件盘点：387 份 Markdown/HTML，其中 2 份是 Vite 应用入口，不作为文档删除对象；未发现字节完全相同的文档副本。
- 主项目 385 份文档的 772 个本地链接、48 个锚点检查通过（排除代码块中的字面量链接）；应用入口的 `/src/main.tsx` 另按 Vite 项目根核对存在。
- 五项措辞修正及 CONTRIBUTING 中命令入口存在性检查通过；仅核对现有脚本定义，本次未运行 Go/Admin/数据库实测，不将文档校验当作运行验收。
- 两套验收标识与任务迁移入口检查通过；原 Admin 里程碑正文、新底座 13 项验收、Cleanup 交互、明确授权边界及正式 Gate 正文比对保持不变，旧真实 Provider 验收仍保留在 ADMIN-WEB-V1。
- `git diff --check`、架构 HTML 标签嵌套与 ID 唯一性检查通过。
- 已有 ADR、p0/p1/legacy/references/standalone、固定 Gate/E2E、契约、SQL、生成物、脚本和来源 manifest 未修改；生成锁直接列出的 3 份 Markdown 输入存在且原字节不变。
- Cleanup 资源名称/generation 确认，生产写入/部署发布/脏 worktree 批准条款，Daytona 固定基线、中英文与内容隔离要求保留。
- 当时仓内未发现 AGENTS.md 或 SKILL.md；该次整合精简已有 CLAUDE 路由，没有修改全局 skills 或其他项目。
- 归档保留原正文并调整相对链接；旧 Admin 归档另追加来自用户原提示词的 ADMIN-WEB-V1 固定验收，不改原里程碑正文。删去的非 Gate 整理报告可从 `ed7d3ac5` 恢复；没有删除 Git 历史、stash 或 worktree。

### 编码约束补充（2026-09-06）

- 用户要求避免反复添加逐版本重复代码。新增根 [AGENTS.md](../../../AGENTS.md) 作为 Codex 入口，复用 [CLAUDE.md](../../../CLAUDE.md#implementation-simplicity) 的单一编码规范，并在 04 的执行入口及 Goal 模板引用；不改变 BASE/ADMIN 验收、原授权或其他任务。
- 静态追踪发现：[生成器](../../../scripts/generate-foundation-migration-package.ts) 只输出当前 binding，[产品 selector](../../../services/control-plane/internal/localmigration/product_runner_binding.go) 手工维护历史数据，[账本校验](../../../services/control-plane/internal/localmigration/localmigration.go) 仍消费历史摘要。规则针对重复维护来源，不要求删除仍有用途的历史绑定或关闭完整性校验。
- 本次仅调整项目指令与文档并核对引用、diff 和原约束；没有重构正在修改的迁移代码、运行迁移/E2E、更新全局设置或自动修改运行中的 Goal。后续代码是否已简化须核对实际实现，不能由本记录推定已完成。

## 3. 当前决定与按需历史

- D-055 / [ADR-0032](../adr/0032-infrastructure-admin-delivery-and-document-routing.md)：当前联合交付与文档路由，已接受用户明确边界。
- D-054 / [ADR-0031](../adr/0031-foundation-first-cloud-workspace-platform.md)：基础设施/应用分层仍适用；Admin 可后补的解释由 D-055 排除。旧文档合并授权不能覆盖用户撤销。
- [旧决策表与固定 Gate registry](history/06-status-tracker-20260905.md)：查询其他适用 ADR、原批准、固定候选和历史 Gate 状态时使用，禁止从其旧“下一步”选择当前工作。
- [旧迁移安全要求](history/04-legacy-migration-plan.md)、[旧 Admin 里程碑](history/07-legacy-admin-milestones.md)、[正式证据规则](evidence/README.md)：只在对应范围查询，不自动加载全文。

## 4. 更新规则

本页记录当前任务范围、阶段、已检查结论、下一项和真实阻塞；不复制长日志或逐提交历史。
BASE/APP 固定证据使用 source/ref、backend、输入、实际结果和未覆盖项；沿用既有报告位置。
正式 Gate 的 VERIFIED 仍需对应 closure、独立评审与签署；本轮文档、普通阶段报告和 BASE-READY 不自动关闭它们。
缺少新的实测证据时保持开放；不要求先有“通过”报告才能开始已授权的修改。
