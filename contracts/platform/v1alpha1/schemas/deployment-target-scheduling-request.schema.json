{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://schemas.cloud-agents.dev/platform/v1alpha1/schemas/deployment-target-scheduling-request.schema.json",
  "title": "DeploymentTargetSchedulingRequest",
  "type": "object",
  "additionalProperties": false,
  "required": ["expectedGeneration", "expectedResourceVersion", "desiredState", "impactDigest"],
  "properties": {
    "expectedGeneration": { "type": "integer", "minimum": 1, "maximum": 9223372036854775807 },
    "expectedResourceVersion": { "$ref": "../../../common/v1alpha1/schemas/resource-version.schema.json" },
    "desiredState": { "enum": ["active", "drained"] },
    "impactDigest": { "type": "string", "pattern": "^sha256:[0-9a-f]{64}$" }
  },
  "x-cloud-agents-semantic-constraints": [
    "all fields must exactly match one current server-authoritative scheduling preview",
    "desiredState must be the opposite of the current Deployment Target scheduling state",
    "impactDigest binds the Target fences and complete active Lease impact without accepting client-authored scope"
  ]
}
