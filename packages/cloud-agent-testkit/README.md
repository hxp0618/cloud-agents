# `@synara/cloud-agent-testkit`

Reusable, host-neutral conformance assertions for Provider descriptors, Protocol v2 command correlation, golden frames, and packed executable validation. `runCloudAgentPackedBinConformance` returns both deterministic passed cases and an explicit `realProviderGates` list; it never reports simulated provider behavior as real-provider acceptance.
