# `@cloud-agents/cloud-agent-provider-codex`

Portable Codex provider implementation for `@cloud-agents/cloud-agent-runtime`. It is explicitly registered by a host or distribution and does not scan user directories or `node_modules`.
