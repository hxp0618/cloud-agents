[project]
name = "cloud-agents-contract-standards"
version = "0.0.0"
requires-python = ">=3.14,<3.15"
dependencies = [
  "jsonschema-rs==0.50.1",
  "openapi-spec-validator==0.9.0",
]

[tool.uv]
package = false
